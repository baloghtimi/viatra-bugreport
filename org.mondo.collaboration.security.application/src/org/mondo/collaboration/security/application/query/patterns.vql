package org.mondo.collaboration.security.application.query

import "http://www.eclipse.org/emf/2002/Ecore"
import "http://www.mondo.org/collaboration/policy/Rules"
import "http://WTSpec4M/5.0"

import java org.mondo.collaboration.security.application.BoundType
import java org.mondo.collaboration.security.application.Constants

pattern permissionOutOfBound(dominantBound: java BoundType, dominantAccess: AccessibilityLevel, dominatedAccess: AccessibilityLevel) = {
	dominantBound == eval(
		if (dominantAccess.value < dominatedAccess.value) 
			BoundType::UPPER
		else if (dominantAccess.value > dominatedAccess.value)
			BoundType::LOWER
		else null
	);
}

pattern readWriteOperation(operation: OperationType) = {
	operation == OperationType::READ;
} or {
	operation == OperationType::WRITE;
}

pattern lowerUpperBound(bound: java BoundType) = {
	bound == eval(BoundType::LOWER);
} or {
	bound == eval(BoundType::UPPER);
}

pattern allUsers(user: java String) {
    user == "Owner";
    } or {
    user == "PumpEngineer";
    } or {
    user == "IOManager";
    } or {
    user == "FanEngineer";
    } or {
    user == "HeaterEngineer";
    } or {
    user == "SystemManager";
    } or {
    user == "WTManager";
}

pattern permitIORuleAsset(io : EObject)
{
    find org.mondo.collaboration.security.application.query.externalIO(io);
}

pattern permitIORuleUser(user: java String)
{
	user == "IOManager";
}

pattern permitIORuleBoundAndPriority(bound: java BoundType, priority: java Integer)
{
    bound == eval(BoundType::LOWER);
    priority == 399;
} or {
    bound == eval(BoundType::UPPER);
    priority == 400;
}

pattern hideModulesRuleAsset(module : EObject)
{
    find org.mondo.collaboration.security.application.query.objectModules(module);
}

pattern hideModulesRuleUser(user: java String)
{
	user == "IOManager";
}

pattern hideModulesRuleBoundAndPriority(bound: java BoundType, priority: java Integer)
{
    bound == eval(BoundType::LOWER);
    priority == 379;
} or {
    bound == eval(BoundType::UPPER);
    priority == 380;
}

pattern hideControlsRuleAsset(ctrl : EObject)
{
    find org.mondo.collaboration.security.application.query.objectControls(ctrl);
}

pattern hideControlsRuleUser(user: java String)
{
	user == "IOManager";
}

pattern hideControlsRuleBoundAndPriority(bound: java BoundType, priority: java Integer)
{
    bound == eval(BoundType::LOWER);
    priority == 379;
} or {
    bound == eval(BoundType::UPPER);
    priority == 380;
}

pattern obfSystemRuleAsset(system : EObject)
{
    find org.mondo.collaboration.security.application.query.wtRoot(system);
}

pattern obfSystemRuleUser(user: java String)
{
	user == "IOManager";
}

pattern obfSystemRuleBoundAndPriority(bound: java BoundType, priority: java Integer)
{
    bound == eval(BoundType::LOWER);
    priority == 359;
} or {
    bound == eval(BoundType::UPPER);
    priority == 360;
}

pattern readOnlyIORuleAsset(io : EObject)
{
    find org.mondo.collaboration.security.application.query.externalIO(io);
}

pattern readOnlyIORuleUser(user: java String)
{
	user == "PumpEngineer";
	} or {
	user == "FanEngineer";
	} or {
	user == "HeaterEngineer";
	} or {
	user == "WTManager";
	} or {
	user == "SystemManager";
}

pattern readOnlyIORuleBoundAndPriority(bound: java BoundType, priority: java Integer)
{
    bound == eval(BoundType::LOWER);
    priority == 279;
} or {
    bound == eval(BoundType::UPPER);
    priority == 280;
}

pattern hideMiscRuleAsset(misc : EObject)
{
    find org.mondo.collaboration.security.application.query.objectMisc(misc);
}

pattern hideMiscRuleUser(user: java String)
{
	user == "IOManager";
	} or {
	user == "WTManager";
}

pattern hideMiscRuleBoundAndPriority(bound: java BoundType, priority: java Integer)
{
    bound == eval(BoundType::LOWER);
    priority == 259;
} or {
    bound == eval(BoundType::UPPER);
    priority == 260;
}

pattern readOnlyMiscRuleAsset(misc : EObject)
{
    find org.mondo.collaboration.security.application.query.objectMisc(misc);
}

pattern readOnlyMiscRuleUser(user: java String)
{
	user == "SystemManager";
}

pattern readOnlyMiscRuleBoundAndPriority(bound: java BoundType, priority: java Integer)
{
    bound == eval(BoundType::LOWER);
    priority == 239;
} or {
    bound == eval(BoundType::UPPER);
    priority == 240;
}

pattern hideDescriptionOfProtectedRuleAsset(module : EObject)
{
    find org.mondo.collaboration.security.application.query.moduleProtected(module);
}

pattern hideDescriptionOfProtectedRuleUser(user: java String)
{
	user == "PumpEngineer";
	} or {
	user == "FanEngineer";
	} or {
	user == "HeaterEngineer";
}

pattern hideDescriptionOfProtectedRuleBoundAndPriority(bound: java BoundType, priority: java Integer)
{
    bound == eval(BoundType::LOWER);
    priority == 199;
} or {
    bound == eval(BoundType::UPPER);
    priority == 200;
}

pattern hideConsumptionByProtectedRuleAsset(ctrl : EObject, signal : EObject, reference : java String)
{
    find org.mondo.collaboration.security.application.query.consumptionProtected(ctrl, signal);
    find attributeAsset(ctrl, signal, reference);
    reference == "inputs";
}

pattern hideConsumptionByProtectedRuleUser(user: java String)
{
	user == "PumpEngineer";
	} or {
	user == "FanEngineer";
	} or {
	user == "HeaterEngineer";
}

pattern hideConsumptionByProtectedRuleBoundAndPriority(bound: java BoundType, priority: java Integer)
{
    bound == eval(BoundType::LOWER);
    priority == 199;
} or {
    bound == eval(BoundType::UPPER);
    priority == 200;
}

pattern ownModulesFANRuleAsset(module : EObject)
{
    find org.mondo.collaboration.security.application.query.moduleOwnedBySpecialist(module, SpecialistKind::FAN);
}

pattern ownModulesFANRuleUser(user: java String)
{
	user == "FanEngineer";
}

pattern ownModulesFANRuleBoundAndPriority(bound: java BoundType, priority: java Integer)
{
    bound == eval(BoundType::LOWER);
    priority == 179;
} or {
    bound == eval(BoundType::UPPER);
    priority == 180;
}

pattern ownModulesHEATERRuleAsset(module : EObject)
{
    find org.mondo.collaboration.security.application.query.moduleOwnedBySpecialist(module, SpecialistKind::HEATER);
}

pattern ownModulesHEATERRuleUser(user: java String)
{
	user == "HeaterEngineer";
}

pattern ownModulesHEATERRuleBoundAndPriority(bound: java BoundType, priority: java Integer)
{
    bound == eval(BoundType::LOWER);
    priority == 179;
} or {
    bound == eval(BoundType::UPPER);
    priority == 180;
}

pattern ownModulesPUMPRuleAsset(module : EObject)
{
    find org.mondo.collaboration.security.application.query.moduleOwnedBySpecialist(module, SpecialistKind::PUMP);
}

pattern ownModulesPUMPRuleUser(user: java String)
{
	user == "PumpEngineer";
}

pattern ownModulesPUMPRuleBoundAndPriority(bound: java BoundType, priority: java Integer)
{
    bound == eval(BoundType::LOWER);
    priority == 179;
} or {
    bound == eval(BoundType::UPPER);
    priority == 180;
}

pattern ownSignalsFANRuleAsset(signal : EObject)
{
    find org.mondo.collaboration.security.application.query.signalOwnedBySpecialist(_, signal, SpecialistKind::FAN);
}

pattern ownSignalsFANRuleUser(user: java String)
{
	user == "FanEngineer";
}

pattern ownSignalsFANRuleBoundAndPriority(bound: java BoundType, priority: java Integer)
{
    bound == eval(BoundType::LOWER);
    priority == 179;
} or {
    bound == eval(BoundType::UPPER);
    priority == 180;
}

pattern ownSignalsHEATERRuleAsset(signal : EObject)
{
    find org.mondo.collaboration.security.application.query.signalOwnedBySpecialist(_, signal, SpecialistKind::HEATER);
}

pattern ownSignalsHEATERRuleUser(user: java String)
{
	user == "HeaterEngineer";
}

pattern ownSignalsHEATERRuleBoundAndPriority(bound: java BoundType, priority: java Integer)
{
    bound == eval(BoundType::LOWER);
    priority == 179;
} or {
    bound == eval(BoundType::UPPER);
    priority == 180;
}

pattern ownSignalsPUMPRuleAsset(signal : EObject)
{
    find org.mondo.collaboration.security.application.query.signalOwnedBySpecialist(_, signal, SpecialistKind::PUMP);
}

pattern ownSignalsPUMPRuleUser(user: java String)
{
	user == "PumpEngineer";
}

pattern ownSignalsPUMPRuleBoundAndPriority(bound: java BoundType, priority: java Integer)
{
    bound == eval(BoundType::LOWER);
    priority == 179;
} or {
    bound == eval(BoundType::UPPER);
    priority == 180;
}

pattern containingModulesFANRuleAsset(container : EObject)
{
    find org.mondo.collaboration.security.application.query.moduleVisibleToSpecialistSubmodule(container, SpecialistKind::FAN);
}

pattern containingModulesFANRuleUser(user: java String)
{
	user == "FanEngineer";
}

pattern containingModulesFANRuleBoundAndPriority(bound: java BoundType, priority: java Integer)
{
    bound == eval(BoundType::LOWER);
    priority == 179;
} or {
    bound == eval(BoundType::UPPER);
    priority == 180;
}

pattern containingModulesHEATERRuleAsset(container : EObject)
{
    find org.mondo.collaboration.security.application.query.moduleVisibleToSpecialistSubmodule(container, SpecialistKind::HEATER);
}

pattern containingModulesHEATERRuleUser(user: java String)
{
	user == "HeaterEngineer";
}

pattern containingModulesHEATERRuleBoundAndPriority(bound: java BoundType, priority: java Integer)
{
    bound == eval(BoundType::LOWER);
    priority == 179;
} or {
    bound == eval(BoundType::UPPER);
    priority == 180;
}

pattern containingModulesPUMPRuleAsset(container : EObject)
{
    find org.mondo.collaboration.security.application.query.moduleVisibleToSpecialistSubmodule(container, SpecialistKind::PUMP);
}

pattern containingModulesPUMPRuleUser(user: java String)
{
	user == "PumpEngineer";
}

pattern containingModulesPUMPRuleBoundAndPriority(bound: java BoundType, priority: java Integer)
{
    bound == eval(BoundType::LOWER);
    priority == 179;
} or {
    bound == eval(BoundType::UPPER);
    priority == 180;
}

pattern siblingSignalsFANRuleAsset(signal : EObject)
{
    find org.mondo.collaboration.security.application.query.signalVisibleToSpecialistSubmodule(_, signal, SpecialistKind::FAN);
}

pattern siblingSignalsFANRuleUser(user: java String)
{
	user == "FanEngineer";
}

pattern siblingSignalsFANRuleBoundAndPriority(bound: java BoundType, priority: java Integer)
{
    bound == eval(BoundType::LOWER);
    priority == 179;
} or {
    bound == eval(BoundType::UPPER);
    priority == 180;
}

pattern siblingSignalsHEATERRuleAsset(signal : EObject)
{
    find org.mondo.collaboration.security.application.query.signalVisibleToSpecialistSubmodule(_, signal, SpecialistKind::HEATER);
}

pattern siblingSignalsHEATERRuleUser(user: java String)
{
	user == "HeaterEngineer";
}

pattern siblingSignalsHEATERRuleBoundAndPriority(bound: java BoundType, priority: java Integer)
{
    bound == eval(BoundType::LOWER);
    priority == 179;
} or {
    bound == eval(BoundType::UPPER);
    priority == 180;
}

pattern siblingSignalsPUMPRuleAsset(signal : EObject)
{
    find org.mondo.collaboration.security.application.query.signalVisibleToSpecialistSubmodule(_, signal, SpecialistKind::PUMP);
}

pattern siblingSignalsPUMPRuleUser(user: java String)
{
	user == "PumpEngineer";
}

pattern siblingSignalsPUMPRuleBoundAndPriority(bound: java BoundType, priority: java Integer)
{
    bound == eval(BoundType::LOWER);
    priority == 179;
} or {
    bound == eval(BoundType::UPPER);
    priority == 180;
}

pattern hideSystemRuleAsset(system : EObject)
{
    find org.mondo.collaboration.security.application.query.wtRoot(system);
}

pattern hideSystemRuleUser(user: java String)
{
	user == "PumpEngineer";
	} or {
	user == "FanEngineer";
	} or {
	user == "HeaterEngineer";
}

pattern hideSystemRuleBoundAndPriority(bound: java BoundType, priority: java Integer)
{
    bound == eval(BoundType::LOWER);
    priority == 159;
} or {
    bound == eval(BoundType::UPPER);
    priority == 160;
}

pattern hideDefaultRuleAsset(root : EObject)
{
    find org.mondo.collaboration.security.application.query.modelRoot(root);
}

pattern hideDefaultRuleUser(user: java String)
{
	user == "PumpEngineer";
	} or {
	user == "FanEngineer";
	} or {
	user == "IOManager";
	} or {
	user == "HeaterEngineer";
	} or {
	user == "SystemManager";
	} or {
	user == "WTManager";
}

pattern hideDefaultRuleBoundAndPriority(bound: java BoundType, priority: java Integer)
{
    bound == eval(BoundType::LOWER);
    priority == 39;
} or {
    bound == eval(BoundType::UPPER);
    priority == 40;
}

pattern fullAccessRuleAsset(root : EObject)
{
    find org.mondo.collaboration.security.application.query.modelRoot(root);
}

pattern fullAccessRuleUser(user: java String)
{
	user == "Owner";
}

pattern fullAccessRuleBoundAndPriority(bound: java BoundType, priority: java Integer)
{
    bound == eval(BoundType::LOWER);
    priority == 19;
} or {
    bound == eval(BoundType::UPPER);
    priority == 20;
}


pattern explicitJudgement_object(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType, priority: java Integer)
{
    //  rule permitIO
    find permitIORuleAsset(object);
    find permitIORuleUser(user);
    find readWriteOperation(operation);
    access == AccessibilityLevel::ALLOW;
    find permitIORuleBoundAndPriority(bound, priority);
    } or {
    //  rule hideModules
    find hideModulesRuleAsset(object);
    find hideModulesRuleUser(user);
    find readWriteOperation(operation);
    access == AccessibilityLevel::DENY;
    find hideModulesRuleBoundAndPriority(bound, priority);
    } or {
    //  rule hideControls
    find hideControlsRuleAsset(object);
    find hideControlsRuleUser(user);
    find readWriteOperation(operation);
    access == AccessibilityLevel::DENY;
    find hideControlsRuleBoundAndPriority(bound, priority);
    } or {
    //  rule obfSystem
    find obfSystemRuleAsset(object);
    find obfSystemRuleUser(user);
    operation == OperationType::READ;
    access == AccessibilityLevel::OBFUSCATE;
    find obfSystemRuleBoundAndPriority(bound, priority);
    } or {
    //  rule readOnlyIO
    find readOnlyIORuleAsset(object);
    find readOnlyIORuleUser(user);
    operation == OperationType::WRITE;
    access == AccessibilityLevel::DENY;
    find readOnlyIORuleBoundAndPriority(bound, priority);
    } or {
    //  rule hideMisc
    find hideMiscRuleAsset(object);
    find hideMiscRuleUser(user);
    find readWriteOperation(operation);
    access == AccessibilityLevel::DENY;
    find hideMiscRuleBoundAndPriority(bound, priority);
    } or {
    //  rule readOnlyMisc
    find readOnlyMiscRuleAsset(object);
    find readOnlyMiscRuleUser(user);
    operation == OperationType::WRITE;
    access == AccessibilityLevel::DENY;
    find readOnlyMiscRuleBoundAndPriority(bound, priority);
    } or {
    //  rule hideDescriptionOfProtected
    find hideDescriptionOfProtectedRuleAsset(object);
    find hideDescriptionOfProtectedRuleUser(user);
    find readWriteOperation(operation);
    access == AccessibilityLevel::DENY;
    find hideDescriptionOfProtectedRuleBoundAndPriority(bound, priority);
    } or {
    //  rule ownModulesFAN
    find ownModulesFANRuleAsset(object);
    find ownModulesFANRuleUser(user);
    find readWriteOperation(operation);
    access == AccessibilityLevel::ALLOW;
    find ownModulesFANRuleBoundAndPriority(bound, priority);
    } or {
    //  rule ownModulesHEATER
    find ownModulesHEATERRuleAsset(object);
    find ownModulesHEATERRuleUser(user);
    find readWriteOperation(operation);
    access == AccessibilityLevel::ALLOW;
    find ownModulesHEATERRuleBoundAndPriority(bound, priority);
    } or {
    //  rule ownModulesPUMP
    find ownModulesPUMPRuleAsset(object);
    find ownModulesPUMPRuleUser(user);
    find readWriteOperation(operation);
    access == AccessibilityLevel::ALLOW;
    find ownModulesPUMPRuleBoundAndPriority(bound, priority);
    } or {
    //  rule ownSignalsFAN
    find ownSignalsFANRuleAsset(object);
    find ownSignalsFANRuleUser(user);
    find readWriteOperation(operation);
    access == AccessibilityLevel::ALLOW;
    find ownSignalsFANRuleBoundAndPriority(bound, priority);
    } or {
    //  rule ownSignalsHEATER
    find ownSignalsHEATERRuleAsset(object);
    find ownSignalsHEATERRuleUser(user);
    find readWriteOperation(operation);
    access == AccessibilityLevel::ALLOW;
    find ownSignalsHEATERRuleBoundAndPriority(bound, priority);
    } or {
    //  rule ownSignalsPUMP
    find ownSignalsPUMPRuleAsset(object);
    find ownSignalsPUMPRuleUser(user);
    find readWriteOperation(operation);
    access == AccessibilityLevel::ALLOW;
    find ownSignalsPUMPRuleBoundAndPriority(bound, priority);
    } or {
    //  rule containingModulesFAN
    find containingModulesFANRuleAsset(object);
    find containingModulesFANRuleUser(user);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    find containingModulesFANRuleBoundAndPriority(bound, priority);
    } or {
    //  rule containingModulesHEATER
    find containingModulesHEATERRuleAsset(object);
    find containingModulesHEATERRuleUser(user);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    find containingModulesHEATERRuleBoundAndPriority(bound, priority);
    } or {
    //  rule containingModulesPUMP
    find containingModulesPUMPRuleAsset(object);
    find containingModulesPUMPRuleUser(user);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    find containingModulesPUMPRuleBoundAndPriority(bound, priority);
    } or {
    //  rule siblingSignalsFAN
    find siblingSignalsFANRuleAsset(object);
    find siblingSignalsFANRuleUser(user);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    find siblingSignalsFANRuleBoundAndPriority(bound, priority);
    } or {
    //  rule siblingSignalsHEATER
    find siblingSignalsHEATERRuleAsset(object);
    find siblingSignalsHEATERRuleUser(user);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    find siblingSignalsHEATERRuleBoundAndPriority(bound, priority);
    } or {
    //  rule siblingSignalsPUMP
    find siblingSignalsPUMPRuleAsset(object);
    find siblingSignalsPUMPRuleUser(user);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    find siblingSignalsPUMPRuleBoundAndPriority(bound, priority);
    } or {
    //  rule hideSystem
    find hideSystemRuleAsset(object);
    find hideSystemRuleUser(user);
    find readWriteOperation(operation);
    access == AccessibilityLevel::DENY;
    find hideSystemRuleBoundAndPriority(bound, priority);
    } or {
    //  rule hideDefault
    find hideDefaultRuleAsset(object);
    find hideDefaultRuleUser(user);
    find readWriteOperation(operation);
    access == AccessibilityLevel::DENY;
    find hideDefaultRuleBoundAndPriority(bound, priority);
    } or {
    //  rule fullAccess
    find fullAccessRuleAsset(object);
    find fullAccessRuleUser(user);
    find readWriteOperation(operation);
    access == AccessibilityLevel::ALLOW;
    find fullAccessRuleBoundAndPriority(bound, priority);
} or {
    //  rule default
    find objectAsset(object);
    find allUsers(user);
    find readWriteOperation(operation);
    access == AccessibilityLevel::DENY;
    find lowerUpperBound(bound);
    priority == eval(Constants::DEFAULT_PRIORITY);
}
pattern effectiveJudgement_object(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find effectiveJudgement_object_default(user, object, operation, access, bound);
    } or {
    find effectiveJudgement_object_weak(user, object, operation, access, bound);
    } or {
    find effectiveJudgement_object_19(user, object, operation, access, bound);
    } or {
    find effectiveJudgement_object_20(user, object, operation, access, bound);
    } or {
    find effectiveJudgement_object_39(user, object, operation, access, bound);
    } or {
    find effectiveJudgement_object_40(user, object, operation, access, bound);
    } or {
    find effectiveJudgement_object_159(user, object, operation, access, bound);
    } or {
    find effectiveJudgement_object_160(user, object, operation, access, bound);
    } or {
    find effectiveJudgement_object_179(user, object, operation, access, bound);
    } or {
    find effectiveJudgement_object_180(user, object, operation, access, bound);
    } or {
    find effectiveJudgement_object_199(user, object, operation, access, bound);
    } or {
    find effectiveJudgement_object_200(user, object, operation, access, bound);
    } or {
    find effectiveJudgement_object_239(user, object, operation, access, bound);
    } or {
    find effectiveJudgement_object_240(user, object, operation, access, bound);
    } or {
    find effectiveJudgement_object_259(user, object, operation, access, bound);
    } or {
    find effectiveJudgement_object_260(user, object, operation, access, bound);
    } or {
    find effectiveJudgement_object_279(user, object, operation, access, bound);
    } or {
    find effectiveJudgement_object_280(user, object, operation, access, bound);
    } or {
    find effectiveJudgement_object_359(user, object, operation, access, bound);
    } or {
    find effectiveJudgement_object_360(user, object, operation, access, bound);
    } or {
    find effectiveJudgement_object_379(user, object, operation, access, bound);
    } or {
    find effectiveJudgement_object_380(user, object, operation, access, bound);
    } or {
    find effectiveJudgement_object_399(user, object, operation, access, bound);
    } or {
    find effectiveJudgement_object_400(user, object, operation, access, bound);
}
    
pattern effectiveJudgement_object_default(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
	find judgement_object_default(user, object, operation, access, bound);
	neg find dominationOf_object_default(user, object, operation, access, _dominantBound);
} 	

pattern effectiveJudgement_object_weak(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
	find judgement_object_weak(user, object, operation, access, bound);
	neg find dominationOf_object_weak(user, object, operation, access, _dominantBound);
} 		

pattern effectiveJudgement_object_19(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_object_19(user, object, operation, access, bound);
    neg find dominationOf_object_19(user, object, operation, access, _dominantBound);
}

pattern effectiveJudgement_object_20(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_object_20(user, object, operation, access, bound);
    neg find dominationOf_object_20(user, object, operation, access, _dominantBound);
}

pattern effectiveJudgement_object_39(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_object_39(user, object, operation, access, bound);
    neg find dominationOf_object_39(user, object, operation, access, _dominantBound);
}

pattern effectiveJudgement_object_40(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_object_40(user, object, operation, access, bound);
    neg find dominationOf_object_40(user, object, operation, access, _dominantBound);
}

pattern effectiveJudgement_object_159(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_object_159(user, object, operation, access, bound);
    neg find dominationOf_object_159(user, object, operation, access, _dominantBound);
}

pattern effectiveJudgement_object_160(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_object_160(user, object, operation, access, bound);
    neg find dominationOf_object_160(user, object, operation, access, _dominantBound);
}

pattern effectiveJudgement_object_179(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_object_179(user, object, operation, access, bound);
    neg find dominationOf_object_179(user, object, operation, access, _dominantBound);
}

pattern effectiveJudgement_object_180(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_object_180(user, object, operation, access, bound);
    neg find dominationOf_object_180(user, object, operation, access, _dominantBound);
}

pattern effectiveJudgement_object_199(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_object_199(user, object, operation, access, bound);
    neg find dominationOf_object_199(user, object, operation, access, _dominantBound);
}

pattern effectiveJudgement_object_200(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_object_200(user, object, operation, access, bound);
    neg find dominationOf_object_200(user, object, operation, access, _dominantBound);
}

pattern effectiveJudgement_object_239(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_object_239(user, object, operation, access, bound);
    neg find dominationOf_object_239(user, object, operation, access, _dominantBound);
}

pattern effectiveJudgement_object_240(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_object_240(user, object, operation, access, bound);
    neg find dominationOf_object_240(user, object, operation, access, _dominantBound);
}

pattern effectiveJudgement_object_259(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_object_259(user, object, operation, access, bound);
    neg find dominationOf_object_259(user, object, operation, access, _dominantBound);
}

pattern effectiveJudgement_object_260(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_object_260(user, object, operation, access, bound);
    neg find dominationOf_object_260(user, object, operation, access, _dominantBound);
}

pattern effectiveJudgement_object_279(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_object_279(user, object, operation, access, bound);
    neg find dominationOf_object_279(user, object, operation, access, _dominantBound);
}

pattern effectiveJudgement_object_280(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_object_280(user, object, operation, access, bound);
    neg find dominationOf_object_280(user, object, operation, access, _dominantBound);
}

pattern effectiveJudgement_object_359(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_object_359(user, object, operation, access, bound);
    neg find dominationOf_object_359(user, object, operation, access, _dominantBound);
}

pattern effectiveJudgement_object_360(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_object_360(user, object, operation, access, bound);
    neg find dominationOf_object_360(user, object, operation, access, _dominantBound);
}

pattern effectiveJudgement_object_379(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_object_379(user, object, operation, access, bound);
    neg find dominationOf_object_379(user, object, operation, access, _dominantBound);
}

pattern effectiveJudgement_object_380(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_object_380(user, object, operation, access, bound);
    neg find dominationOf_object_380(user, object, operation, access, _dominantBound);
}

pattern effectiveJudgement_object_399(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_object_399(user, object, operation, access, bound);
    neg find dominationOf_object_399(user, object, operation, access, _dominantBound);
}

pattern effectiveJudgement_object_400(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_object_400(user, object, operation, access, bound);
}


pattern dominationOf_object_default(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_object_weak(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_19(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_20(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_39(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_40(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_159(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_160(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_179(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_180(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_199(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_200(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_239(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_240(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_259(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_260(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_279(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_280(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_359(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_360(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_379(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_380(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_399(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_400(user, object, operation, dominatedAccess, dominantAccess);
} 

pattern dominationOf_object_weak(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_object_19(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_20(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_39(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_40(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_159(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_160(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_179(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_180(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_199(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_200(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_239(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_240(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_259(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_260(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_279(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_280(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_359(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_360(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_379(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_380(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_399(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_400(user, object, operation, dominatedAccess, dominantAccess);
} 

pattern dominationOf_object_19(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_object_20(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_39(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_40(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_159(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_160(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_179(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_180(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_199(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_200(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_239(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_240(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_259(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_260(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_279(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_280(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_359(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_360(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_379(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_380(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_399(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_400(user, object, operation, dominatedAccess, dominantAccess);
}
pattern dominationOf_object_20(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_object_39(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_40(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_159(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_160(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_179(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_180(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_199(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_200(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_239(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_240(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_259(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_260(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_279(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_280(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_359(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_360(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_379(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_380(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_399(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_400(user, object, operation, dominatedAccess, dominantAccess);
}
pattern dominationOf_object_39(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_object_40(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_159(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_160(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_179(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_180(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_199(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_200(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_239(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_240(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_259(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_260(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_279(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_280(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_359(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_360(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_379(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_380(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_399(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_400(user, object, operation, dominatedAccess, dominantAccess);
}
pattern dominationOf_object_40(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_object_159(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_160(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_179(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_180(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_199(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_200(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_239(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_240(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_259(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_260(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_279(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_280(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_359(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_360(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_379(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_380(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_399(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_400(user, object, operation, dominatedAccess, dominantAccess);
}
pattern dominationOf_object_159(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_object_160(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_179(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_180(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_199(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_200(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_239(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_240(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_259(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_260(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_279(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_280(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_359(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_360(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_379(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_380(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_399(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_400(user, object, operation, dominatedAccess, dominantAccess);
}
pattern dominationOf_object_160(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_object_179(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_180(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_199(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_200(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_239(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_240(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_259(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_260(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_279(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_280(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_359(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_360(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_379(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_380(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_399(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_400(user, object, operation, dominatedAccess, dominantAccess);
}
pattern dominationOf_object_179(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_object_180(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_199(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_200(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_239(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_240(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_259(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_260(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_279(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_280(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_359(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_360(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_379(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_380(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_399(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_400(user, object, operation, dominatedAccess, dominantAccess);
}
pattern dominationOf_object_180(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_object_199(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_200(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_239(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_240(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_259(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_260(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_279(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_280(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_359(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_360(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_379(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_380(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_399(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_400(user, object, operation, dominatedAccess, dominantAccess);
}
pattern dominationOf_object_199(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_object_200(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_239(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_240(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_259(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_260(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_279(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_280(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_359(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_360(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_379(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_380(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_399(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_400(user, object, operation, dominatedAccess, dominantAccess);
}
pattern dominationOf_object_200(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_object_239(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_240(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_259(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_260(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_279(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_280(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_359(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_360(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_379(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_380(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_399(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_400(user, object, operation, dominatedAccess, dominantAccess);
}
pattern dominationOf_object_239(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_object_240(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_259(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_260(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_279(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_280(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_359(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_360(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_379(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_380(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_399(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_400(user, object, operation, dominatedAccess, dominantAccess);
}
pattern dominationOf_object_240(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_object_259(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_260(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_279(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_280(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_359(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_360(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_379(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_380(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_399(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_400(user, object, operation, dominatedAccess, dominantAccess);
}
pattern dominationOf_object_259(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_object_260(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_279(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_280(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_359(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_360(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_379(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_380(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_399(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_400(user, object, operation, dominatedAccess, dominantAccess);
}
pattern dominationOf_object_260(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_object_279(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_280(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_359(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_360(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_379(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_380(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_399(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_400(user, object, operation, dominatedAccess, dominantAccess);
}
pattern dominationOf_object_279(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_object_280(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_359(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_360(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_379(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_380(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_399(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_400(user, object, operation, dominatedAccess, dominantAccess);
}
pattern dominationOf_object_280(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_object_359(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_360(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_379(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_380(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_399(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_400(user, object, operation, dominatedAccess, dominantAccess);
}
pattern dominationOf_object_359(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_object_360(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_379(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_380(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_399(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_400(user, object, operation, dominatedAccess, dominantAccess);
}
pattern dominationOf_object_360(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_object_379(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_380(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_399(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_400(user, object, operation, dominatedAccess, dominantAccess);
}
pattern dominationOf_object_379(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_object_380(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_399(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_400(user, object, operation, dominatedAccess, dominantAccess);
}
pattern dominationOf_object_380(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_object_399(user, object, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_object_400(user, object, operation, dominatedAccess, dominantAccess);
}
pattern dominationOf_object_399(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_object_400(user, object, operation, dominatedAccess, dominantAccess);
}
pattern dominationBy_object_weak(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_object_weak(user, object, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  

pattern dominationBy_object_19(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_object_19(user, object, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_object_20(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_object_20(user, object, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_object_39(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_object_39(user, object, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_object_40(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_object_40(user, object, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_object_159(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_object_159(user, object, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_object_160(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_object_160(user, object, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_object_179(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_object_179(user, object, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_object_180(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_object_180(user, object, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_object_199(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_object_199(user, object, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_object_200(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_object_200(user, object, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_object_239(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_object_239(user, object, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_object_240(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_object_240(user, object, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_object_259(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_object_259(user, object, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_object_260(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_object_260(user, object, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_object_279(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_object_279(user, object, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_object_280(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_object_280(user, object, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_object_359(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_object_359(user, object, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_object_360(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_object_360(user, object, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_object_379(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_object_379(user, object, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_object_380(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_object_380(user, object, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_object_399(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_object_399(user, object, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_object_400(user: java String, object: EObject, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_object_400(user, object, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  

pattern judgement_object_default(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find explicitJudgement_object(user, object, operation, access, bound, eval(Constants::DEFAULT_PRIORITY));
} or {
	find relaxedJudgement_object_default(user, object, operation, access, bound);
}

pattern judgement_object_weak(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find relaxedJudgement_object_weak(user, object, operation, access, bound);
} or {
	find strongConsequenceJudgement_object_weak(user, object, operation, access, bound);
} or {
    find weakConsequenceJudgement_object(user, object, operation, access, bound);
}

pattern judgement_object_19(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find explicitJudgement_object(user, object, operation, access, bound, 19);
    } or {
	find relaxedJudgement_object_19(user, object, operation, access, bound);
	} or {
	find strongConsequenceJudgement_object_19(user, object, operation, access, bound);
}

pattern judgement_object_20(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find explicitJudgement_object(user, object, operation, access, bound, 20);
    } or {
	find relaxedJudgement_object_20(user, object, operation, access, bound);
	} or {
	find strongConsequenceJudgement_object_20(user, object, operation, access, bound);
}

pattern judgement_object_39(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find explicitJudgement_object(user, object, operation, access, bound, 39);
    } or {
	find relaxedJudgement_object_39(user, object, operation, access, bound);
	} or {
	find strongConsequenceJudgement_object_39(user, object, operation, access, bound);
}

pattern judgement_object_40(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find explicitJudgement_object(user, object, operation, access, bound, 40);
    } or {
	find relaxedJudgement_object_40(user, object, operation, access, bound);
	} or {
	find strongConsequenceJudgement_object_40(user, object, operation, access, bound);
}

pattern judgement_object_159(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find explicitJudgement_object(user, object, operation, access, bound, 159);
    } or {
	find relaxedJudgement_object_159(user, object, operation, access, bound);
	} or {
	find strongConsequenceJudgement_object_159(user, object, operation, access, bound);
}

pattern judgement_object_160(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find explicitJudgement_object(user, object, operation, access, bound, 160);
    } or {
	find relaxedJudgement_object_160(user, object, operation, access, bound);
	} or {
	find strongConsequenceJudgement_object_160(user, object, operation, access, bound);
}

pattern judgement_object_179(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find explicitJudgement_object(user, object, operation, access, bound, 179);
    } or {
	find relaxedJudgement_object_179(user, object, operation, access, bound);
	} or {
	find strongConsequenceJudgement_object_179(user, object, operation, access, bound);
}

pattern judgement_object_180(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find explicitJudgement_object(user, object, operation, access, bound, 180);
    } or {
	find relaxedJudgement_object_180(user, object, operation, access, bound);
	} or {
	find strongConsequenceJudgement_object_180(user, object, operation, access, bound);
}

pattern judgement_object_199(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find explicitJudgement_object(user, object, operation, access, bound, 199);
    } or {
	find relaxedJudgement_object_199(user, object, operation, access, bound);
	} or {
	find strongConsequenceJudgement_object_199(user, object, operation, access, bound);
}

pattern judgement_object_200(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find explicitJudgement_object(user, object, operation, access, bound, 200);
    } or {
	find relaxedJudgement_object_200(user, object, operation, access, bound);
	} or {
	find strongConsequenceJudgement_object_200(user, object, operation, access, bound);
}

pattern judgement_object_239(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find explicitJudgement_object(user, object, operation, access, bound, 239);
    } or {
	find relaxedJudgement_object_239(user, object, operation, access, bound);
	} or {
	find strongConsequenceJudgement_object_239(user, object, operation, access, bound);
}

pattern judgement_object_240(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find explicitJudgement_object(user, object, operation, access, bound, 240);
    } or {
	find relaxedJudgement_object_240(user, object, operation, access, bound);
	} or {
	find strongConsequenceJudgement_object_240(user, object, operation, access, bound);
}

pattern judgement_object_259(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find explicitJudgement_object(user, object, operation, access, bound, 259);
    } or {
	find relaxedJudgement_object_259(user, object, operation, access, bound);
	} or {
	find strongConsequenceJudgement_object_259(user, object, operation, access, bound);
}

pattern judgement_object_260(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find explicitJudgement_object(user, object, operation, access, bound, 260);
    } or {
	find relaxedJudgement_object_260(user, object, operation, access, bound);
	} or {
	find strongConsequenceJudgement_object_260(user, object, operation, access, bound);
}

pattern judgement_object_279(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find explicitJudgement_object(user, object, operation, access, bound, 279);
    } or {
	find relaxedJudgement_object_279(user, object, operation, access, bound);
	} or {
	find strongConsequenceJudgement_object_279(user, object, operation, access, bound);
}

pattern judgement_object_280(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find explicitJudgement_object(user, object, operation, access, bound, 280);
    } or {
	find relaxedJudgement_object_280(user, object, operation, access, bound);
	} or {
	find strongConsequenceJudgement_object_280(user, object, operation, access, bound);
}

pattern judgement_object_359(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find explicitJudgement_object(user, object, operation, access, bound, 359);
    } or {
	find relaxedJudgement_object_359(user, object, operation, access, bound);
	} or {
	find strongConsequenceJudgement_object_359(user, object, operation, access, bound);
}

pattern judgement_object_360(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find explicitJudgement_object(user, object, operation, access, bound, 360);
    } or {
	find relaxedJudgement_object_360(user, object, operation, access, bound);
	} or {
	find strongConsequenceJudgement_object_360(user, object, operation, access, bound);
}

pattern judgement_object_379(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find explicitJudgement_object(user, object, operation, access, bound, 379);
    } or {
	find relaxedJudgement_object_379(user, object, operation, access, bound);
	} or {
	find strongConsequenceJudgement_object_379(user, object, operation, access, bound);
}

pattern judgement_object_380(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find explicitJudgement_object(user, object, operation, access, bound, 380);
    } or {
	find relaxedJudgement_object_380(user, object, operation, access, bound);
	} or {
	find strongConsequenceJudgement_object_380(user, object, operation, access, bound);
}

pattern judgement_object_399(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find explicitJudgement_object(user, object, operation, access, bound, 399);
    } or {
	find relaxedJudgement_object_399(user, object, operation, access, bound);
	} or {
	find strongConsequenceJudgement_object_399(user, object, operation, access, bound);
}

pattern judgement_object_400(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find explicitJudgement_object(user, object, operation, access, bound, 400);
    } or {
	find strongConsequenceJudgement_object_400(user, object, operation, access, bound);
}


pattern relaxedJudgement_object_default(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_object_default(user, object, operation, dominatedAccess, bound);
    find dominationOf_object_default(user, object, operation, _dominatedAccess, access);
}

pattern relaxedJudgement_object_weak(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_object_weak(user, object, operation, dominatedAccess, bound);
    find dominationOf_object_weak(user, object, operation, _dominatedAccess, access);
}

pattern relaxedJudgement_object_19(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_object_19(user, object, operation, dominatedAccess, bound);
    find dominationOf_object_19(user, object, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_object_20(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_object_20(user, object, operation, dominatedAccess, bound);
    find dominationOf_object_20(user, object, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_object_39(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_object_39(user, object, operation, dominatedAccess, bound);
    find dominationOf_object_39(user, object, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_object_40(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_object_40(user, object, operation, dominatedAccess, bound);
    find dominationOf_object_40(user, object, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_object_159(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_object_159(user, object, operation, dominatedAccess, bound);
    find dominationOf_object_159(user, object, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_object_160(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_object_160(user, object, operation, dominatedAccess, bound);
    find dominationOf_object_160(user, object, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_object_179(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_object_179(user, object, operation, dominatedAccess, bound);
    find dominationOf_object_179(user, object, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_object_180(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_object_180(user, object, operation, dominatedAccess, bound);
    find dominationOf_object_180(user, object, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_object_199(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_object_199(user, object, operation, dominatedAccess, bound);
    find dominationOf_object_199(user, object, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_object_200(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_object_200(user, object, operation, dominatedAccess, bound);
    find dominationOf_object_200(user, object, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_object_239(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_object_239(user, object, operation, dominatedAccess, bound);
    find dominationOf_object_239(user, object, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_object_240(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_object_240(user, object, operation, dominatedAccess, bound);
    find dominationOf_object_240(user, object, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_object_259(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_object_259(user, object, operation, dominatedAccess, bound);
    find dominationOf_object_259(user, object, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_object_260(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_object_260(user, object, operation, dominatedAccess, bound);
    find dominationOf_object_260(user, object, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_object_279(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_object_279(user, object, operation, dominatedAccess, bound);
    find dominationOf_object_279(user, object, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_object_280(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_object_280(user, object, operation, dominatedAccess, bound);
    find dominationOf_object_280(user, object, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_object_359(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_object_359(user, object, operation, dominatedAccess, bound);
    find dominationOf_object_359(user, object, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_object_360(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_object_360(user, object, operation, dominatedAccess, bound);
    find dominationOf_object_360(user, object, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_object_379(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_object_379(user, object, operation, dominatedAccess, bound);
    find dominationOf_object_379(user, object, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_object_380(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_object_380(user, object, operation, dominatedAccess, bound);
    find dominationOf_object_380(user, object, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_object_399(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_object_399(user, object, operation, dominatedAccess, bound);
    find dominationOf_object_399(user, object, operation, _dominatedAccess, access);
}
pattern strongConsequenceJudgement_object_weak(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // object W -> object R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_weak(user, object, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// child R -> parent O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_weak(user, child, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// attribute R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_attribute_weak(user, object, _value, _attribute, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_weak(user, object, _target, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> target O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_weak(user, _source, object, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// containment reference W -> target W
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_weak(user, anySource, object, anyReference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(anySource, object, anyReference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object !R -> object !W
	dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_weak(user, object, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
    // containment reference !R -> target !R
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_reference_weak(user, anySource, object, anyReference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
    find containmentReference(anySource, object, anyReference);
    operation == OperationType::READ;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
} or {
	// object O -> object !W
	find effectiveJudgement_object_weak(user, object, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
} or {
	// child O -> parent O
	find effectiveJudgement_object_weak(user, child, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
}

pattern strongConsequenceJudgement_object_19(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // object W -> object R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_19(user, object, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// child R -> parent O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_19(user, child, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// attribute R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_attribute_19(user, object, _value, _attribute, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_19(user, object, _target, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> target O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_19(user, _source, object, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// containment reference W -> target W
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_19(user, anySource, object, anyReference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(anySource, object, anyReference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object !R -> object !W
	dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_19(user, object, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
    // containment reference !R -> target !R
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_reference_19(user, anySource, object, anyReference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
    find containmentReference(anySource, object, anyReference);
    operation == OperationType::READ;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
} or {
	// object O -> object !W
	find effectiveJudgement_object_19(user, object, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
} or {
	// child O -> parent O
	find effectiveJudgement_object_19(user, child, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
}
pattern strongConsequenceJudgement_object_20(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // object W -> object R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_20(user, object, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// child R -> parent O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_20(user, child, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// attribute R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_attribute_20(user, object, _value, _attribute, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_20(user, object, _target, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> target O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_20(user, _source, object, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// containment reference W -> target W
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_20(user, anySource, object, anyReference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(anySource, object, anyReference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object !R -> object !W
	dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_20(user, object, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
    // containment reference !R -> target !R
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_reference_20(user, anySource, object, anyReference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
    find containmentReference(anySource, object, anyReference);
    operation == OperationType::READ;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
} or {
	// object O -> object !W
	find effectiveJudgement_object_20(user, object, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
} or {
	// child O -> parent O
	find effectiveJudgement_object_20(user, child, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
}
pattern strongConsequenceJudgement_object_39(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // object W -> object R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_39(user, object, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// child R -> parent O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_39(user, child, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// attribute R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_attribute_39(user, object, _value, _attribute, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_39(user, object, _target, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> target O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_39(user, _source, object, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// containment reference W -> target W
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_39(user, anySource, object, anyReference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(anySource, object, anyReference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object !R -> object !W
	dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_39(user, object, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
    // containment reference !R -> target !R
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_reference_39(user, anySource, object, anyReference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
    find containmentReference(anySource, object, anyReference);
    operation == OperationType::READ;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
} or {
	// object O -> object !W
	find effectiveJudgement_object_39(user, object, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
} or {
	// child O -> parent O
	find effectiveJudgement_object_39(user, child, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
}
pattern strongConsequenceJudgement_object_40(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // object W -> object R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_40(user, object, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// child R -> parent O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_40(user, child, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// attribute R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_attribute_40(user, object, _value, _attribute, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_40(user, object, _target, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> target O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_40(user, _source, object, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// containment reference W -> target W
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_40(user, anySource, object, anyReference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(anySource, object, anyReference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object !R -> object !W
	dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_40(user, object, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
    // containment reference !R -> target !R
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_reference_40(user, anySource, object, anyReference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
    find containmentReference(anySource, object, anyReference);
    operation == OperationType::READ;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
} or {
	// object O -> object !W
	find effectiveJudgement_object_40(user, object, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
} or {
	// child O -> parent O
	find effectiveJudgement_object_40(user, child, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
}
pattern strongConsequenceJudgement_object_159(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // object W -> object R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_159(user, object, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// child R -> parent O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_159(user, child, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// attribute R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_attribute_159(user, object, _value, _attribute, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_159(user, object, _target, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> target O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_159(user, _source, object, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// containment reference W -> target W
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_159(user, anySource, object, anyReference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(anySource, object, anyReference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object !R -> object !W
	dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_159(user, object, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
    // containment reference !R -> target !R
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_reference_159(user, anySource, object, anyReference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
    find containmentReference(anySource, object, anyReference);
    operation == OperationType::READ;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
} or {
	// object O -> object !W
	find effectiveJudgement_object_159(user, object, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
} or {
	// child O -> parent O
	find effectiveJudgement_object_159(user, child, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
}
pattern strongConsequenceJudgement_object_160(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // object W -> object R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_160(user, object, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// child R -> parent O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_160(user, child, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// attribute R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_attribute_160(user, object, _value, _attribute, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_160(user, object, _target, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> target O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_160(user, _source, object, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// containment reference W -> target W
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_160(user, anySource, object, anyReference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(anySource, object, anyReference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object !R -> object !W
	dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_160(user, object, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
    // containment reference !R -> target !R
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_reference_160(user, anySource, object, anyReference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
    find containmentReference(anySource, object, anyReference);
    operation == OperationType::READ;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
} or {
	// object O -> object !W
	find effectiveJudgement_object_160(user, object, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
} or {
	// child O -> parent O
	find effectiveJudgement_object_160(user, child, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
}
pattern strongConsequenceJudgement_object_179(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // object W -> object R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_179(user, object, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// child R -> parent O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_179(user, child, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// attribute R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_attribute_179(user, object, _value, _attribute, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_179(user, object, _target, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> target O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_179(user, _source, object, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// containment reference W -> target W
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_179(user, anySource, object, anyReference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(anySource, object, anyReference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object !R -> object !W
	dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_179(user, object, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
    // containment reference !R -> target !R
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_reference_179(user, anySource, object, anyReference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
    find containmentReference(anySource, object, anyReference);
    operation == OperationType::READ;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
} or {
	// object O -> object !W
	find effectiveJudgement_object_179(user, object, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
} or {
	// child O -> parent O
	find effectiveJudgement_object_179(user, child, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
}
pattern strongConsequenceJudgement_object_180(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // object W -> object R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_180(user, object, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// child R -> parent O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_180(user, child, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// attribute R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_attribute_180(user, object, _value, _attribute, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_180(user, object, _target, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> target O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_180(user, _source, object, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// containment reference W -> target W
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_180(user, anySource, object, anyReference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(anySource, object, anyReference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object !R -> object !W
	dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_180(user, object, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
    // containment reference !R -> target !R
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_reference_180(user, anySource, object, anyReference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
    find containmentReference(anySource, object, anyReference);
    operation == OperationType::READ;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
} or {
	// object O -> object !W
	find effectiveJudgement_object_180(user, object, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
} or {
	// child O -> parent O
	find effectiveJudgement_object_180(user, child, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
}
pattern strongConsequenceJudgement_object_199(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // object W -> object R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_199(user, object, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// child R -> parent O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_199(user, child, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// attribute R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_attribute_199(user, object, _value, _attribute, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_199(user, object, _target, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> target O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_199(user, _source, object, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// containment reference W -> target W
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_199(user, anySource, object, anyReference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(anySource, object, anyReference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object !R -> object !W
	dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_199(user, object, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
    // containment reference !R -> target !R
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_reference_199(user, anySource, object, anyReference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
    find containmentReference(anySource, object, anyReference);
    operation == OperationType::READ;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
} or {
	// object O -> object !W
	find effectiveJudgement_object_199(user, object, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
} or {
	// child O -> parent O
	find effectiveJudgement_object_199(user, child, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
}
pattern strongConsequenceJudgement_object_200(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // object W -> object R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_200(user, object, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// child R -> parent O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_200(user, child, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// attribute R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_attribute_200(user, object, _value, _attribute, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_200(user, object, _target, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> target O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_200(user, _source, object, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// containment reference W -> target W
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_200(user, anySource, object, anyReference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(anySource, object, anyReference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object !R -> object !W
	dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_200(user, object, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
    // containment reference !R -> target !R
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_reference_200(user, anySource, object, anyReference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
    find containmentReference(anySource, object, anyReference);
    operation == OperationType::READ;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
} or {
	// object O -> object !W
	find effectiveJudgement_object_200(user, object, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
} or {
	// child O -> parent O
	find effectiveJudgement_object_200(user, child, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
}
pattern strongConsequenceJudgement_object_239(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // object W -> object R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_239(user, object, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// child R -> parent O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_239(user, child, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// attribute R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_attribute_239(user, object, _value, _attribute, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_239(user, object, _target, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> target O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_239(user, _source, object, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// containment reference W -> target W
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_239(user, anySource, object, anyReference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(anySource, object, anyReference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object !R -> object !W
	dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_239(user, object, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
    // containment reference !R -> target !R
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_reference_239(user, anySource, object, anyReference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
    find containmentReference(anySource, object, anyReference);
    operation == OperationType::READ;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
} or {
	// object O -> object !W
	find effectiveJudgement_object_239(user, object, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
} or {
	// child O -> parent O
	find effectiveJudgement_object_239(user, child, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
}
pattern strongConsequenceJudgement_object_240(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // object W -> object R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_240(user, object, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// child R -> parent O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_240(user, child, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// attribute R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_attribute_240(user, object, _value, _attribute, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_240(user, object, _target, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> target O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_240(user, _source, object, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// containment reference W -> target W
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_240(user, anySource, object, anyReference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(anySource, object, anyReference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object !R -> object !W
	dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_240(user, object, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
    // containment reference !R -> target !R
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_reference_240(user, anySource, object, anyReference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
    find containmentReference(anySource, object, anyReference);
    operation == OperationType::READ;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
} or {
	// object O -> object !W
	find effectiveJudgement_object_240(user, object, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
} or {
	// child O -> parent O
	find effectiveJudgement_object_240(user, child, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
}
pattern strongConsequenceJudgement_object_259(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // object W -> object R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_259(user, object, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// child R -> parent O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_259(user, child, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// attribute R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_attribute_259(user, object, _value, _attribute, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_259(user, object, _target, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> target O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_259(user, _source, object, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// containment reference W -> target W
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_259(user, anySource, object, anyReference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(anySource, object, anyReference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object !R -> object !W
	dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_259(user, object, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
    // containment reference !R -> target !R
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_reference_259(user, anySource, object, anyReference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
    find containmentReference(anySource, object, anyReference);
    operation == OperationType::READ;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
} or {
	// object O -> object !W
	find effectiveJudgement_object_259(user, object, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
} or {
	// child O -> parent O
	find effectiveJudgement_object_259(user, child, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
}
pattern strongConsequenceJudgement_object_260(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // object W -> object R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_260(user, object, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// child R -> parent O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_260(user, child, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// attribute R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_attribute_260(user, object, _value, _attribute, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_260(user, object, _target, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> target O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_260(user, _source, object, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// containment reference W -> target W
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_260(user, anySource, object, anyReference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(anySource, object, anyReference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object !R -> object !W
	dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_260(user, object, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
    // containment reference !R -> target !R
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_reference_260(user, anySource, object, anyReference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
    find containmentReference(anySource, object, anyReference);
    operation == OperationType::READ;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
} or {
	// object O -> object !W
	find effectiveJudgement_object_260(user, object, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
} or {
	// child O -> parent O
	find effectiveJudgement_object_260(user, child, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
}
pattern strongConsequenceJudgement_object_279(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // object W -> object R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_279(user, object, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// child R -> parent O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_279(user, child, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// attribute R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_attribute_279(user, object, _value, _attribute, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_279(user, object, _target, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> target O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_279(user, _source, object, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// containment reference W -> target W
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_279(user, anySource, object, anyReference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(anySource, object, anyReference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object !R -> object !W
	dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_279(user, object, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
    // containment reference !R -> target !R
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_reference_279(user, anySource, object, anyReference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
    find containmentReference(anySource, object, anyReference);
    operation == OperationType::READ;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
} or {
	// object O -> object !W
	find effectiveJudgement_object_279(user, object, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
} or {
	// child O -> parent O
	find effectiveJudgement_object_279(user, child, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
}
pattern strongConsequenceJudgement_object_280(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // object W -> object R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_280(user, object, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// child R -> parent O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_280(user, child, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// attribute R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_attribute_280(user, object, _value, _attribute, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_280(user, object, _target, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> target O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_280(user, _source, object, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// containment reference W -> target W
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_280(user, anySource, object, anyReference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(anySource, object, anyReference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object !R -> object !W
	dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_280(user, object, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
    // containment reference !R -> target !R
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_reference_280(user, anySource, object, anyReference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
    find containmentReference(anySource, object, anyReference);
    operation == OperationType::READ;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
} or {
	// object O -> object !W
	find effectiveJudgement_object_280(user, object, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
} or {
	// child O -> parent O
	find effectiveJudgement_object_280(user, child, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
}
pattern strongConsequenceJudgement_object_359(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // object W -> object R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_359(user, object, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// child R -> parent O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_359(user, child, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// attribute R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_attribute_359(user, object, _value, _attribute, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_359(user, object, _target, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> target O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_359(user, _source, object, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// containment reference W -> target W
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_359(user, anySource, object, anyReference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(anySource, object, anyReference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object !R -> object !W
	dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_359(user, object, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
    // containment reference !R -> target !R
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_reference_359(user, anySource, object, anyReference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
    find containmentReference(anySource, object, anyReference);
    operation == OperationType::READ;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
} or {
	// object O -> object !W
	find effectiveJudgement_object_359(user, object, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
} or {
	// child O -> parent O
	find effectiveJudgement_object_359(user, child, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
}
pattern strongConsequenceJudgement_object_360(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // object W -> object R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_360(user, object, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// child R -> parent O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_360(user, child, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// attribute R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_attribute_360(user, object, _value, _attribute, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_360(user, object, _target, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> target O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_360(user, _source, object, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// containment reference W -> target W
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_360(user, anySource, object, anyReference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(anySource, object, anyReference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object !R -> object !W
	dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_360(user, object, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
    // containment reference !R -> target !R
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_reference_360(user, anySource, object, anyReference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
    find containmentReference(anySource, object, anyReference);
    operation == OperationType::READ;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
} or {
	// object O -> object !W
	find effectiveJudgement_object_360(user, object, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
} or {
	// child O -> parent O
	find effectiveJudgement_object_360(user, child, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
}
pattern strongConsequenceJudgement_object_379(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // object W -> object R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_379(user, object, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// child R -> parent O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_379(user, child, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// attribute R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_attribute_379(user, object, _value, _attribute, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_379(user, object, _target, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> target O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_379(user, _source, object, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// containment reference W -> target W
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_379(user, anySource, object, anyReference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(anySource, object, anyReference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object !R -> object !W
	dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_379(user, object, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
    // containment reference !R -> target !R
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_reference_379(user, anySource, object, anyReference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
    find containmentReference(anySource, object, anyReference);
    operation == OperationType::READ;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
} or {
	// object O -> object !W
	find effectiveJudgement_object_379(user, object, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
} or {
	// child O -> parent O
	find effectiveJudgement_object_379(user, child, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
}
pattern strongConsequenceJudgement_object_380(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // object W -> object R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_380(user, object, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// child R -> parent O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_380(user, child, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// attribute R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_attribute_380(user, object, _value, _attribute, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_380(user, object, _target, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> target O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_380(user, _source, object, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// containment reference W -> target W
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_380(user, anySource, object, anyReference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(anySource, object, anyReference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object !R -> object !W
	dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_380(user, object, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
    // containment reference !R -> target !R
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_reference_380(user, anySource, object, anyReference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
    find containmentReference(anySource, object, anyReference);
    operation == OperationType::READ;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
} or {
	// object O -> object !W
	find effectiveJudgement_object_380(user, object, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
} or {
	// child O -> parent O
	find effectiveJudgement_object_380(user, child, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
}
pattern strongConsequenceJudgement_object_399(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // object W -> object R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_399(user, object, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// child R -> parent O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_399(user, child, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// attribute R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_attribute_399(user, object, _value, _attribute, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_399(user, object, _target, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> target O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_399(user, _source, object, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// containment reference W -> target W
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_399(user, anySource, object, anyReference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(anySource, object, anyReference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object !R -> object !W
	dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_399(user, object, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
    // containment reference !R -> target !R
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_reference_399(user, anySource, object, anyReference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
    find containmentReference(anySource, object, anyReference);
    operation == OperationType::READ;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
} or {
	// object O -> object !W
	find effectiveJudgement_object_399(user, object, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
} or {
	// child O -> parent O
	find effectiveJudgement_object_399(user, child, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
}
pattern strongConsequenceJudgement_object_400(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // object W -> object R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_400(user, object, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// child R -> parent O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_400(user, child, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// attribute R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_attribute_400(user, object, _value, _attribute, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> source O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_400(user, object, _target, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// reference R -> target O
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_400(user, _source, object, _reference, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
	bound == eval(BoundType::LOWER);
} or {
	// containment reference W -> target W
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_reference_400(user, anySource, object, anyReference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(anySource, object, anyReference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object !R -> object !W
	dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_400(user, object, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
    // containment reference !R -> target !R
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_reference_400(user, anySource, object, anyReference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
    find containmentReference(anySource, object, anyReference);
    operation == OperationType::READ;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
} or {
	// object O -> object !W
	find effectiveJudgement_object_400(user, object, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
} or {
	// child O -> parent O
	find effectiveJudgement_object_400(user, child, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	find contains(object, child);
	operation == OperationType::READ;
	access == AccessibilityLevel::OBFUSCATE;
}

pattern weakConsequenceJudgement_object(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find weakConsequenceJudgement_object_weak(user, object, operation, access, bound);
	} or {
    find weakConsequenceJudgement_object_19(user, object, operation, access, bound);
    } or {
    find weakConsequenceJudgement_object_20(user, object, operation, access, bound);
    } or {
    find weakConsequenceJudgement_object_39(user, object, operation, access, bound);
    } or {
    find weakConsequenceJudgement_object_40(user, object, operation, access, bound);
    } or {
    find weakConsequenceJudgement_object_159(user, object, operation, access, bound);
    } or {
    find weakConsequenceJudgement_object_160(user, object, operation, access, bound);
    } or {
    find weakConsequenceJudgement_object_179(user, object, operation, access, bound);
    } or {
    find weakConsequenceJudgement_object_180(user, object, operation, access, bound);
    } or {
    find weakConsequenceJudgement_object_199(user, object, operation, access, bound);
    } or {
    find weakConsequenceJudgement_object_200(user, object, operation, access, bound);
    } or {
    find weakConsequenceJudgement_object_239(user, object, operation, access, bound);
    } or {
    find weakConsequenceJudgement_object_240(user, object, operation, access, bound);
    } or {
    find weakConsequenceJudgement_object_259(user, object, operation, access, bound);
    } or {
    find weakConsequenceJudgement_object_260(user, object, operation, access, bound);
    } or {
    find weakConsequenceJudgement_object_279(user, object, operation, access, bound);
    } or {
    find weakConsequenceJudgement_object_280(user, object, operation, access, bound);
    } or {
    find weakConsequenceJudgement_object_359(user, object, operation, access, bound);
    } or {
    find weakConsequenceJudgement_object_360(user, object, operation, access, bound);
    } or {
    find weakConsequenceJudgement_object_379(user, object, operation, access, bound);
    } or {
    find weakConsequenceJudgement_object_380(user, object, operation, access, bound);
    } or {
    find weakConsequenceJudgement_object_399(user, object, operation, access, bound);
    } or {
    find weakConsequenceJudgement_object_400(user, object, operation, access, bound);
}

pattern weakConsequenceJudgement_object_weak(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_weak(user, container, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(container, object);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_weak(user, container, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
    find contains(container, object);
    operation == OperationType::WRITE;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
}

pattern weakConsequenceJudgement_object_19(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_19(user, container, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(container, object);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_19(user, container, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
    find contains(container, object);
    operation == OperationType::WRITE;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_object_20(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_20(user, container, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(container, object);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_20(user, container, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
    find contains(container, object);
    operation == OperationType::WRITE;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_object_39(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_39(user, container, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(container, object);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_39(user, container, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
    find contains(container, object);
    operation == OperationType::WRITE;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_object_40(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_40(user, container, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(container, object);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_40(user, container, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
    find contains(container, object);
    operation == OperationType::WRITE;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_object_159(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_159(user, container, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(container, object);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_159(user, container, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
    find contains(container, object);
    operation == OperationType::WRITE;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_object_160(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_160(user, container, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(container, object);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_160(user, container, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
    find contains(container, object);
    operation == OperationType::WRITE;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_object_179(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_179(user, container, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(container, object);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_179(user, container, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
    find contains(container, object);
    operation == OperationType::WRITE;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_object_180(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_180(user, container, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(container, object);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_180(user, container, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
    find contains(container, object);
    operation == OperationType::WRITE;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_object_199(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_199(user, container, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(container, object);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_199(user, container, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
    find contains(container, object);
    operation == OperationType::WRITE;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_object_200(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_200(user, container, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(container, object);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_200(user, container, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
    find contains(container, object);
    operation == OperationType::WRITE;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_object_239(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_239(user, container, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(container, object);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_239(user, container, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
    find contains(container, object);
    operation == OperationType::WRITE;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_object_240(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_240(user, container, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(container, object);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_240(user, container, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
    find contains(container, object);
    operation == OperationType::WRITE;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_object_259(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_259(user, container, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(container, object);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_259(user, container, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
    find contains(container, object);
    operation == OperationType::WRITE;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_object_260(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_260(user, container, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(container, object);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_260(user, container, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
    find contains(container, object);
    operation == OperationType::WRITE;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_object_279(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_279(user, container, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(container, object);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_279(user, container, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
    find contains(container, object);
    operation == OperationType::WRITE;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_object_280(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_280(user, container, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(container, object);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_280(user, container, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
    find contains(container, object);
    operation == OperationType::WRITE;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_object_359(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_359(user, container, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(container, object);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_359(user, container, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
    find contains(container, object);
    operation == OperationType::WRITE;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_object_360(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_360(user, container, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(container, object);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_360(user, container, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
    find contains(container, object);
    operation == OperationType::WRITE;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_object_379(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_379(user, container, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(container, object);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_379(user, container, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
    find contains(container, object);
    operation == OperationType::WRITE;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_object_380(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_380(user, container, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(container, object);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_380(user, container, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
    find contains(container, object);
    operation == OperationType::WRITE;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_object_399(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_399(user, container, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(container, object);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_399(user, container, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
    find contains(container, object);
    operation == OperationType::WRITE;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_object_400(user: java String, object: EObject, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_400(user, container, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find contains(container, object);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
    dominantBound == eval(BoundType::UPPER);
    find effectiveJudgement_object_400(user, container, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
    find contains(container, object);
    operation == OperationType::WRITE;
    access == AccessibilityLevel::DENY;
    bound == eval(BoundType::UPPER);
}


pattern explicitJudgement_attribute(user : java String, source : EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType, priority: java Integer)
{
    //  rule default
    find attributeAsset(source, value, attribute);
    find allUsers(user);
    find readWriteOperation(operation);
    access == AccessibilityLevel::DENY;
    find lowerUpperBound(bound);
    priority == eval(Constants::DEFAULT_PRIORITY);
}

pattern effectiveJudgement_attribute(user: java String, source: EObject, value: java Object, attribute: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find effectiveJudgement_attribute_default(user, source, value, attribute, operation, access, bound);
} or {
    find effectiveJudgement_attribute_weak(user, source, value, attribute, operation, access, bound);
} or {
    find effectiveJudgement_attribute_19(user, source, value, attribute, operation, access, bound);
    } or {
    find effectiveJudgement_attribute_20(user, source, value, attribute, operation, access, bound);
    } or {
    find effectiveJudgement_attribute_39(user, source, value, attribute, operation, access, bound);
    } or {
    find effectiveJudgement_attribute_40(user, source, value, attribute, operation, access, bound);
    } or {
    find effectiveJudgement_attribute_159(user, source, value, attribute, operation, access, bound);
    } or {
    find effectiveJudgement_attribute_160(user, source, value, attribute, operation, access, bound);
    } or {
    find effectiveJudgement_attribute_179(user, source, value, attribute, operation, access, bound);
    } or {
    find effectiveJudgement_attribute_180(user, source, value, attribute, operation, access, bound);
    } or {
    find effectiveJudgement_attribute_199(user, source, value, attribute, operation, access, bound);
    } or {
    find effectiveJudgement_attribute_200(user, source, value, attribute, operation, access, bound);
    } or {
    find effectiveJudgement_attribute_239(user, source, value, attribute, operation, access, bound);
    } or {
    find effectiveJudgement_attribute_240(user, source, value, attribute, operation, access, bound);
    } or {
    find effectiveJudgement_attribute_259(user, source, value, attribute, operation, access, bound);
    } or {
    find effectiveJudgement_attribute_260(user, source, value, attribute, operation, access, bound);
    } or {
    find effectiveJudgement_attribute_279(user, source, value, attribute, operation, access, bound);
    } or {
    find effectiveJudgement_attribute_280(user, source, value, attribute, operation, access, bound);
    } or {
    find effectiveJudgement_attribute_359(user, source, value, attribute, operation, access, bound);
    } or {
    find effectiveJudgement_attribute_360(user, source, value, attribute, operation, access, bound);
    } or {
    find effectiveJudgement_attribute_379(user, source, value, attribute, operation, access, bound);
    } or {
    find effectiveJudgement_attribute_380(user, source, value, attribute, operation, access, bound);
    } or {
    find effectiveJudgement_attribute_399(user, source, value, attribute, operation, access, bound);
    } or {
    find effectiveJudgement_attribute_400(user, source, value, attribute, operation, access, bound);
}
    
pattern effectiveJudgement_attribute_default(user: java String, source: EObject, value: java Object, attribute: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
	find judgement_attribute_default(user, source, value, attribute, operation, access, bound);
	neg find dominationOf_attribute_default(user, source, value, attribute, operation, access, _dominantBound);
}

pattern effectiveJudgement_attribute_weak(user: java String, source: EObject, value: java Object, attribute: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
	find judgement_attribute_weak(user, source, value, attribute, operation, access, bound);
	neg find dominationOf_attribute_weak(user, source, value, attribute, operation, access, _dominantBound);
}

pattern effectiveJudgement_attribute_19(user: java String, source: EObject, value: java Object, attribute: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_attribute_19(user, source, value, attribute, operation, access, bound);
    neg find dominationOf_attribute_19(user, source, value, attribute, operation, access, _dominantBound);
}
pattern effectiveJudgement_attribute_20(user: java String, source: EObject, value: java Object, attribute: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_attribute_20(user, source, value, attribute, operation, access, bound);
    neg find dominationOf_attribute_20(user, source, value, attribute, operation, access, _dominantBound);
}
pattern effectiveJudgement_attribute_39(user: java String, source: EObject, value: java Object, attribute: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_attribute_39(user, source, value, attribute, operation, access, bound);
    neg find dominationOf_attribute_39(user, source, value, attribute, operation, access, _dominantBound);
}
pattern effectiveJudgement_attribute_40(user: java String, source: EObject, value: java Object, attribute: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_attribute_40(user, source, value, attribute, operation, access, bound);
    neg find dominationOf_attribute_40(user, source, value, attribute, operation, access, _dominantBound);
}
pattern effectiveJudgement_attribute_159(user: java String, source: EObject, value: java Object, attribute: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_attribute_159(user, source, value, attribute, operation, access, bound);
    neg find dominationOf_attribute_159(user, source, value, attribute, operation, access, _dominantBound);
}
pattern effectiveJudgement_attribute_160(user: java String, source: EObject, value: java Object, attribute: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_attribute_160(user, source, value, attribute, operation, access, bound);
    neg find dominationOf_attribute_160(user, source, value, attribute, operation, access, _dominantBound);
}
pattern effectiveJudgement_attribute_179(user: java String, source: EObject, value: java Object, attribute: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_attribute_179(user, source, value, attribute, operation, access, bound);
    neg find dominationOf_attribute_179(user, source, value, attribute, operation, access, _dominantBound);
}
pattern effectiveJudgement_attribute_180(user: java String, source: EObject, value: java Object, attribute: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_attribute_180(user, source, value, attribute, operation, access, bound);
    neg find dominationOf_attribute_180(user, source, value, attribute, operation, access, _dominantBound);
}
pattern effectiveJudgement_attribute_199(user: java String, source: EObject, value: java Object, attribute: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_attribute_199(user, source, value, attribute, operation, access, bound);
    neg find dominationOf_attribute_199(user, source, value, attribute, operation, access, _dominantBound);
}
pattern effectiveJudgement_attribute_200(user: java String, source: EObject, value: java Object, attribute: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_attribute_200(user, source, value, attribute, operation, access, bound);
    neg find dominationOf_attribute_200(user, source, value, attribute, operation, access, _dominantBound);
}
pattern effectiveJudgement_attribute_239(user: java String, source: EObject, value: java Object, attribute: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_attribute_239(user, source, value, attribute, operation, access, bound);
    neg find dominationOf_attribute_239(user, source, value, attribute, operation, access, _dominantBound);
}
pattern effectiveJudgement_attribute_240(user: java String, source: EObject, value: java Object, attribute: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_attribute_240(user, source, value, attribute, operation, access, bound);
    neg find dominationOf_attribute_240(user, source, value, attribute, operation, access, _dominantBound);
}
pattern effectiveJudgement_attribute_259(user: java String, source: EObject, value: java Object, attribute: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_attribute_259(user, source, value, attribute, operation, access, bound);
    neg find dominationOf_attribute_259(user, source, value, attribute, operation, access, _dominantBound);
}
pattern effectiveJudgement_attribute_260(user: java String, source: EObject, value: java Object, attribute: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_attribute_260(user, source, value, attribute, operation, access, bound);
    neg find dominationOf_attribute_260(user, source, value, attribute, operation, access, _dominantBound);
}
pattern effectiveJudgement_attribute_279(user: java String, source: EObject, value: java Object, attribute: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_attribute_279(user, source, value, attribute, operation, access, bound);
    neg find dominationOf_attribute_279(user, source, value, attribute, operation, access, _dominantBound);
}
pattern effectiveJudgement_attribute_280(user: java String, source: EObject, value: java Object, attribute: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_attribute_280(user, source, value, attribute, operation, access, bound);
    neg find dominationOf_attribute_280(user, source, value, attribute, operation, access, _dominantBound);
}
pattern effectiveJudgement_attribute_359(user: java String, source: EObject, value: java Object, attribute: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_attribute_359(user, source, value, attribute, operation, access, bound);
    neg find dominationOf_attribute_359(user, source, value, attribute, operation, access, _dominantBound);
}
pattern effectiveJudgement_attribute_360(user: java String, source: EObject, value: java Object, attribute: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_attribute_360(user, source, value, attribute, operation, access, bound);
    neg find dominationOf_attribute_360(user, source, value, attribute, operation, access, _dominantBound);
}
pattern effectiveJudgement_attribute_379(user: java String, source: EObject, value: java Object, attribute: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_attribute_379(user, source, value, attribute, operation, access, bound);
    neg find dominationOf_attribute_379(user, source, value, attribute, operation, access, _dominantBound);
}
pattern effectiveJudgement_attribute_380(user: java String, source: EObject, value: java Object, attribute: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_attribute_380(user, source, value, attribute, operation, access, bound);
    neg find dominationOf_attribute_380(user, source, value, attribute, operation, access, _dominantBound);
}
pattern effectiveJudgement_attribute_399(user: java String, source: EObject, value: java Object, attribute: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_attribute_399(user, source, value, attribute, operation, access, bound);
    neg find dominationOf_attribute_399(user, source, value, attribute, operation, access, _dominantBound);
}
pattern effectiveJudgement_attribute_400(user: java String, source: EObject, value: java Object, attribute: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_attribute_400(user, source, value, attribute, operation, access, bound);
}

pattern dominationOf_attribute_default(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_attribute_weak(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_19(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_20(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_39(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_40(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_159(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_160(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_179(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_180(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_199(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_200(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_239(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_240(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_259(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_260(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_279(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_280(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_359(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_360(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_379(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_380(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_399(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_400(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
} 

pattern dominationOf_attribute_weak(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_attribute_19(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_20(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_39(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_40(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_159(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_160(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_179(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_180(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_199(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_200(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_239(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_240(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_259(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_260(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_279(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_280(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_359(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_360(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_379(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_380(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_399(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_400(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
} 

pattern dominationOf_attribute_19(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_attribute_20(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_39(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_40(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_159(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_160(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_179(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_180(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_199(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_200(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_239(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_240(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_259(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_260(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_279(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_280(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_359(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_360(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_379(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_380(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_399(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_400(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_attribute_20(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_attribute_39(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_40(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_159(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_160(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_179(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_180(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_199(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_200(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_239(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_240(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_259(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_260(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_279(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_280(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_359(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_360(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_379(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_380(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_399(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_400(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_attribute_39(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_attribute_40(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_159(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_160(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_179(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_180(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_199(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_200(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_239(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_240(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_259(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_260(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_279(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_280(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_359(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_360(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_379(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_380(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_399(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_400(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_attribute_40(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_attribute_159(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_160(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_179(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_180(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_199(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_200(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_239(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_240(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_259(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_260(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_279(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_280(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_359(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_360(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_379(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_380(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_399(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_400(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_attribute_159(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_attribute_160(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_179(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_180(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_199(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_200(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_239(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_240(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_259(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_260(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_279(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_280(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_359(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_360(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_379(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_380(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_399(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_400(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_attribute_160(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_attribute_179(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_180(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_199(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_200(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_239(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_240(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_259(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_260(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_279(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_280(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_359(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_360(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_379(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_380(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_399(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_400(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_attribute_179(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_attribute_180(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_199(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_200(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_239(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_240(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_259(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_260(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_279(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_280(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_359(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_360(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_379(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_380(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_399(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_400(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_attribute_180(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_attribute_199(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_200(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_239(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_240(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_259(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_260(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_279(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_280(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_359(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_360(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_379(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_380(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_399(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_400(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_attribute_199(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_attribute_200(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_239(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_240(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_259(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_260(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_279(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_280(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_359(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_360(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_379(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_380(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_399(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_400(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_attribute_200(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_attribute_239(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_240(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_259(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_260(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_279(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_280(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_359(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_360(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_379(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_380(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_399(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_400(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_attribute_239(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_attribute_240(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_259(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_260(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_279(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_280(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_359(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_360(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_379(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_380(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_399(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_400(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_attribute_240(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_attribute_259(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_260(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_279(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_280(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_359(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_360(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_379(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_380(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_399(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_400(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_attribute_259(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_attribute_260(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_279(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_280(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_359(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_360(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_379(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_380(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_399(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_400(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_attribute_260(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_attribute_279(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_280(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_359(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_360(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_379(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_380(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_399(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_400(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_attribute_279(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_attribute_280(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_359(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_360(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_379(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_380(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_399(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_400(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_attribute_280(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_attribute_359(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_360(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_379(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_380(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_399(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_400(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_attribute_359(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_attribute_360(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_379(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_380(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_399(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_400(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_attribute_360(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_attribute_379(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_380(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_399(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_400(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_attribute_379(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_attribute_380(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_399(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_400(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_attribute_380(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_attribute_399(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_attribute_400(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_attribute_399(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_attribute_400(user, source, value, attribute, operation, dominatedAccess, dominantAccess);
} 
pattern dominationBy_attribute_weak(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_attribute_weak(user, source, value, attribute, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  

pattern dominationBy_attribute_19(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_attribute_19(user, source, value, attribute, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_attribute_20(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_attribute_20(user, source, value, attribute, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_attribute_39(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_attribute_39(user, source, value, attribute, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_attribute_40(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_attribute_40(user, source, value, attribute, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_attribute_159(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_attribute_159(user, source, value, attribute, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_attribute_160(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_attribute_160(user, source, value, attribute, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_attribute_179(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_attribute_179(user, source, value, attribute, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_attribute_180(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_attribute_180(user, source, value, attribute, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_attribute_199(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_attribute_199(user, source, value, attribute, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_attribute_200(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_attribute_200(user, source, value, attribute, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_attribute_239(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_attribute_239(user, source, value, attribute, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_attribute_240(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_attribute_240(user, source, value, attribute, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_attribute_259(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_attribute_259(user, source, value, attribute, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_attribute_260(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_attribute_260(user, source, value, attribute, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_attribute_279(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_attribute_279(user, source, value, attribute, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_attribute_280(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_attribute_280(user, source, value, attribute, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_attribute_359(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_attribute_359(user, source, value, attribute, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_attribute_360(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_attribute_360(user, source, value, attribute, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_attribute_379(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_attribute_379(user, source, value, attribute, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_attribute_380(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_attribute_380(user, source, value, attribute, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_attribute_399(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_attribute_399(user, source, value, attribute, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_attribute_400(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_attribute_400(user, source, value, attribute, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  

pattern judgement_attribute_default(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find explicitJudgement_attribute(user, source, value, attribute, operation, access, bound, eval(Constants::DEFAULT_PRIORITY));
} or {
	find relaxedJudgement_attribute_default(user, source, value, attribute, operation, access, bound);
}

pattern judgement_attribute_weak(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find relaxedJudgement_attribute_weak(user, source, value, attribute, operation, access, bound);
} or {
	find strongConsequenceJudgement_attribute_weak(user, source, value, attribute, operation, access, bound);
} or {
    find weakConsequenceJudgement_attribute(user, source, value, attribute, operation, access, bound);
}

pattern judgement_attribute_19(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find relaxedJudgement_attribute_19(user, source, value, attribute, operation, access, bound);
	} or {
	find strongConsequenceJudgement_attribute_19(user, source, value, attribute, operation, access, bound);
}

pattern judgement_attribute_20(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find relaxedJudgement_attribute_20(user, source, value, attribute, operation, access, bound);
	} or {
	find strongConsequenceJudgement_attribute_20(user, source, value, attribute, operation, access, bound);
}

pattern judgement_attribute_39(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find relaxedJudgement_attribute_39(user, source, value, attribute, operation, access, bound);
	} or {
	find strongConsequenceJudgement_attribute_39(user, source, value, attribute, operation, access, bound);
}

pattern judgement_attribute_40(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find relaxedJudgement_attribute_40(user, source, value, attribute, operation, access, bound);
	} or {
	find strongConsequenceJudgement_attribute_40(user, source, value, attribute, operation, access, bound);
}

pattern judgement_attribute_159(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find relaxedJudgement_attribute_159(user, source, value, attribute, operation, access, bound);
	} or {
	find strongConsequenceJudgement_attribute_159(user, source, value, attribute, operation, access, bound);
}

pattern judgement_attribute_160(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find relaxedJudgement_attribute_160(user, source, value, attribute, operation, access, bound);
	} or {
	find strongConsequenceJudgement_attribute_160(user, source, value, attribute, operation, access, bound);
}

pattern judgement_attribute_179(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find relaxedJudgement_attribute_179(user, source, value, attribute, operation, access, bound);
	} or {
	find strongConsequenceJudgement_attribute_179(user, source, value, attribute, operation, access, bound);
}

pattern judgement_attribute_180(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find relaxedJudgement_attribute_180(user, source, value, attribute, operation, access, bound);
	} or {
	find strongConsequenceJudgement_attribute_180(user, source, value, attribute, operation, access, bound);
}

pattern judgement_attribute_199(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find relaxedJudgement_attribute_199(user, source, value, attribute, operation, access, bound);
	} or {
	find strongConsequenceJudgement_attribute_199(user, source, value, attribute, operation, access, bound);
}

pattern judgement_attribute_200(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find relaxedJudgement_attribute_200(user, source, value, attribute, operation, access, bound);
	} or {
	find strongConsequenceJudgement_attribute_200(user, source, value, attribute, operation, access, bound);
}

pattern judgement_attribute_239(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find relaxedJudgement_attribute_239(user, source, value, attribute, operation, access, bound);
	} or {
	find strongConsequenceJudgement_attribute_239(user, source, value, attribute, operation, access, bound);
}

pattern judgement_attribute_240(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find relaxedJudgement_attribute_240(user, source, value, attribute, operation, access, bound);
	} or {
	find strongConsequenceJudgement_attribute_240(user, source, value, attribute, operation, access, bound);
}

pattern judgement_attribute_259(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find relaxedJudgement_attribute_259(user, source, value, attribute, operation, access, bound);
	} or {
	find strongConsequenceJudgement_attribute_259(user, source, value, attribute, operation, access, bound);
}

pattern judgement_attribute_260(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find relaxedJudgement_attribute_260(user, source, value, attribute, operation, access, bound);
	} or {
	find strongConsequenceJudgement_attribute_260(user, source, value, attribute, operation, access, bound);
}

pattern judgement_attribute_279(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find relaxedJudgement_attribute_279(user, source, value, attribute, operation, access, bound);
	} or {
	find strongConsequenceJudgement_attribute_279(user, source, value, attribute, operation, access, bound);
}

pattern judgement_attribute_280(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find relaxedJudgement_attribute_280(user, source, value, attribute, operation, access, bound);
	} or {
	find strongConsequenceJudgement_attribute_280(user, source, value, attribute, operation, access, bound);
}

pattern judgement_attribute_359(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find relaxedJudgement_attribute_359(user, source, value, attribute, operation, access, bound);
	} or {
	find strongConsequenceJudgement_attribute_359(user, source, value, attribute, operation, access, bound);
}

pattern judgement_attribute_360(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find relaxedJudgement_attribute_360(user, source, value, attribute, operation, access, bound);
	} or {
	find strongConsequenceJudgement_attribute_360(user, source, value, attribute, operation, access, bound);
}

pattern judgement_attribute_379(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find relaxedJudgement_attribute_379(user, source, value, attribute, operation, access, bound);
	} or {
	find strongConsequenceJudgement_attribute_379(user, source, value, attribute, operation, access, bound);
}

pattern judgement_attribute_380(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find relaxedJudgement_attribute_380(user, source, value, attribute, operation, access, bound);
	} or {
	find strongConsequenceJudgement_attribute_380(user, source, value, attribute, operation, access, bound);
}

pattern judgement_attribute_399(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find relaxedJudgement_attribute_399(user, source, value, attribute, operation, access, bound);
	} or {
	find strongConsequenceJudgement_attribute_399(user, source, value, attribute, operation, access, bound);
}

pattern judgement_attribute_400(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find strongConsequenceJudgement_attribute_400(user, source, value, attribute, operation, access, bound);
}


pattern relaxedJudgement_attribute_default(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_attribute_default(user, source, value, attribute, operation, dominatedAccess, bound);
    find dominationOf_attribute_default(user, source, value, attribute, operation, _dominatedAccess, access);
}

pattern relaxedJudgement_attribute_weak(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_attribute_weak(user, source, value, attribute, operation, dominatedAccess, bound);
    find dominationOf_attribute_weak(user, source, value, attribute, operation, _dominatedAccess, access);
}

pattern relaxedJudgement_attribute_19(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_attribute_19(user, source, value, attribute, operation, dominatedAccess, bound);
    find dominationOf_attribute_19(user, source, value, attribute, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_attribute_20(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_attribute_20(user, source, value, attribute, operation, dominatedAccess, bound);
    find dominationOf_attribute_20(user, source, value, attribute, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_attribute_39(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_attribute_39(user, source, value, attribute, operation, dominatedAccess, bound);
    find dominationOf_attribute_39(user, source, value, attribute, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_attribute_40(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_attribute_40(user, source, value, attribute, operation, dominatedAccess, bound);
    find dominationOf_attribute_40(user, source, value, attribute, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_attribute_159(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_attribute_159(user, source, value, attribute, operation, dominatedAccess, bound);
    find dominationOf_attribute_159(user, source, value, attribute, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_attribute_160(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_attribute_160(user, source, value, attribute, operation, dominatedAccess, bound);
    find dominationOf_attribute_160(user, source, value, attribute, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_attribute_179(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_attribute_179(user, source, value, attribute, operation, dominatedAccess, bound);
    find dominationOf_attribute_179(user, source, value, attribute, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_attribute_180(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_attribute_180(user, source, value, attribute, operation, dominatedAccess, bound);
    find dominationOf_attribute_180(user, source, value, attribute, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_attribute_199(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_attribute_199(user, source, value, attribute, operation, dominatedAccess, bound);
    find dominationOf_attribute_199(user, source, value, attribute, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_attribute_200(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_attribute_200(user, source, value, attribute, operation, dominatedAccess, bound);
    find dominationOf_attribute_200(user, source, value, attribute, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_attribute_239(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_attribute_239(user, source, value, attribute, operation, dominatedAccess, bound);
    find dominationOf_attribute_239(user, source, value, attribute, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_attribute_240(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_attribute_240(user, source, value, attribute, operation, dominatedAccess, bound);
    find dominationOf_attribute_240(user, source, value, attribute, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_attribute_259(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_attribute_259(user, source, value, attribute, operation, dominatedAccess, bound);
    find dominationOf_attribute_259(user, source, value, attribute, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_attribute_260(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_attribute_260(user, source, value, attribute, operation, dominatedAccess, bound);
    find dominationOf_attribute_260(user, source, value, attribute, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_attribute_279(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_attribute_279(user, source, value, attribute, operation, dominatedAccess, bound);
    find dominationOf_attribute_279(user, source, value, attribute, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_attribute_280(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_attribute_280(user, source, value, attribute, operation, dominatedAccess, bound);
    find dominationOf_attribute_280(user, source, value, attribute, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_attribute_359(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_attribute_359(user, source, value, attribute, operation, dominatedAccess, bound);
    find dominationOf_attribute_359(user, source, value, attribute, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_attribute_360(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_attribute_360(user, source, value, attribute, operation, dominatedAccess, bound);
    find dominationOf_attribute_360(user, source, value, attribute, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_attribute_379(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_attribute_379(user, source, value, attribute, operation, dominatedAccess, bound);
    find dominationOf_attribute_379(user, source, value, attribute, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_attribute_380(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_attribute_380(user, source, value, attribute, operation, dominatedAccess, bound);
    find dominationOf_attribute_380(user, source, value, attribute, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_attribute_399(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_attribute_399(user, source, value, attribute, operation, dominatedAccess, bound);
    find dominationOf_attribute_399(user, source, value, attribute, operation, _dominatedAccess, access);
}
pattern strongConsequenceJudgement_attribute_weak(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // attribute W -> attribute R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_attribute_weak(user, source, value, attribute, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// attribute !R -> attribute !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_attribute_weak(user, source, value, attribute, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> attribute !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_weak(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// attribute O -> attribute !W
	find effectiveJudgement_attribute_weak(user, source, value, attribute, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
}

pattern strongConsequenceJudgement_attribute_19(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // attribute W -> attribute R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_attribute_19(user, source, value, attribute, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// attribute !R -> attribute !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_attribute_19(user, source, value, attribute, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> attribute !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_19(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// attribute O -> attribute !W
	find effectiveJudgement_attribute_19(user, source, value, attribute, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
}
pattern strongConsequenceJudgement_attribute_20(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // attribute W -> attribute R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_attribute_20(user, source, value, attribute, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// attribute !R -> attribute !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_attribute_20(user, source, value, attribute, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> attribute !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_20(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// attribute O -> attribute !W
	find effectiveJudgement_attribute_20(user, source, value, attribute, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
}
pattern strongConsequenceJudgement_attribute_39(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // attribute W -> attribute R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_attribute_39(user, source, value, attribute, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// attribute !R -> attribute !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_attribute_39(user, source, value, attribute, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> attribute !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_39(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// attribute O -> attribute !W
	find effectiveJudgement_attribute_39(user, source, value, attribute, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
}
pattern strongConsequenceJudgement_attribute_40(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // attribute W -> attribute R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_attribute_40(user, source, value, attribute, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// attribute !R -> attribute !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_attribute_40(user, source, value, attribute, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> attribute !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_40(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// attribute O -> attribute !W
	find effectiveJudgement_attribute_40(user, source, value, attribute, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
}
pattern strongConsequenceJudgement_attribute_159(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // attribute W -> attribute R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_attribute_159(user, source, value, attribute, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// attribute !R -> attribute !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_attribute_159(user, source, value, attribute, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> attribute !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_159(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// attribute O -> attribute !W
	find effectiveJudgement_attribute_159(user, source, value, attribute, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
}
pattern strongConsequenceJudgement_attribute_160(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // attribute W -> attribute R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_attribute_160(user, source, value, attribute, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// attribute !R -> attribute !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_attribute_160(user, source, value, attribute, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> attribute !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_160(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// attribute O -> attribute !W
	find effectiveJudgement_attribute_160(user, source, value, attribute, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
}
pattern strongConsequenceJudgement_attribute_179(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // attribute W -> attribute R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_attribute_179(user, source, value, attribute, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// attribute !R -> attribute !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_attribute_179(user, source, value, attribute, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> attribute !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_179(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// attribute O -> attribute !W
	find effectiveJudgement_attribute_179(user, source, value, attribute, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
}
pattern strongConsequenceJudgement_attribute_180(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // attribute W -> attribute R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_attribute_180(user, source, value, attribute, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// attribute !R -> attribute !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_attribute_180(user, source, value, attribute, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> attribute !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_180(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// attribute O -> attribute !W
	find effectiveJudgement_attribute_180(user, source, value, attribute, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
}
pattern strongConsequenceJudgement_attribute_199(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // attribute W -> attribute R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_attribute_199(user, source, value, attribute, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// attribute !R -> attribute !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_attribute_199(user, source, value, attribute, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> attribute !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_199(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// attribute O -> attribute !W
	find effectiveJudgement_attribute_199(user, source, value, attribute, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
}
pattern strongConsequenceJudgement_attribute_200(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // attribute W -> attribute R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_attribute_200(user, source, value, attribute, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// attribute !R -> attribute !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_attribute_200(user, source, value, attribute, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> attribute !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_200(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// attribute O -> attribute !W
	find effectiveJudgement_attribute_200(user, source, value, attribute, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
}
pattern strongConsequenceJudgement_attribute_239(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // attribute W -> attribute R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_attribute_239(user, source, value, attribute, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// attribute !R -> attribute !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_attribute_239(user, source, value, attribute, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> attribute !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_239(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// attribute O -> attribute !W
	find effectiveJudgement_attribute_239(user, source, value, attribute, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
}
pattern strongConsequenceJudgement_attribute_240(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // attribute W -> attribute R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_attribute_240(user, source, value, attribute, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// attribute !R -> attribute !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_attribute_240(user, source, value, attribute, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> attribute !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_240(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// attribute O -> attribute !W
	find effectiveJudgement_attribute_240(user, source, value, attribute, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
}
pattern strongConsequenceJudgement_attribute_259(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // attribute W -> attribute R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_attribute_259(user, source, value, attribute, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// attribute !R -> attribute !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_attribute_259(user, source, value, attribute, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> attribute !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_259(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// attribute O -> attribute !W
	find effectiveJudgement_attribute_259(user, source, value, attribute, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
}
pattern strongConsequenceJudgement_attribute_260(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // attribute W -> attribute R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_attribute_260(user, source, value, attribute, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// attribute !R -> attribute !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_attribute_260(user, source, value, attribute, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> attribute !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_260(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// attribute O -> attribute !W
	find effectiveJudgement_attribute_260(user, source, value, attribute, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
}
pattern strongConsequenceJudgement_attribute_279(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // attribute W -> attribute R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_attribute_279(user, source, value, attribute, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// attribute !R -> attribute !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_attribute_279(user, source, value, attribute, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> attribute !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_279(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// attribute O -> attribute !W
	find effectiveJudgement_attribute_279(user, source, value, attribute, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
}
pattern strongConsequenceJudgement_attribute_280(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // attribute W -> attribute R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_attribute_280(user, source, value, attribute, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// attribute !R -> attribute !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_attribute_280(user, source, value, attribute, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> attribute !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_280(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// attribute O -> attribute !W
	find effectiveJudgement_attribute_280(user, source, value, attribute, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
}
pattern strongConsequenceJudgement_attribute_359(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // attribute W -> attribute R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_attribute_359(user, source, value, attribute, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// attribute !R -> attribute !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_attribute_359(user, source, value, attribute, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> attribute !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_359(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// attribute O -> attribute !W
	find effectiveJudgement_attribute_359(user, source, value, attribute, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
}
pattern strongConsequenceJudgement_attribute_360(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // attribute W -> attribute R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_attribute_360(user, source, value, attribute, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// attribute !R -> attribute !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_attribute_360(user, source, value, attribute, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> attribute !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_360(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// attribute O -> attribute !W
	find effectiveJudgement_attribute_360(user, source, value, attribute, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
}
pattern strongConsequenceJudgement_attribute_379(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // attribute W -> attribute R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_attribute_379(user, source, value, attribute, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// attribute !R -> attribute !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_attribute_379(user, source, value, attribute, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> attribute !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_379(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// attribute O -> attribute !W
	find effectiveJudgement_attribute_379(user, source, value, attribute, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
}
pattern strongConsequenceJudgement_attribute_380(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // attribute W -> attribute R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_attribute_380(user, source, value, attribute, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// attribute !R -> attribute !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_attribute_380(user, source, value, attribute, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> attribute !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_380(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// attribute O -> attribute !W
	find effectiveJudgement_attribute_380(user, source, value, attribute, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
}
pattern strongConsequenceJudgement_attribute_399(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // attribute W -> attribute R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_attribute_399(user, source, value, attribute, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// attribute !R -> attribute !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_attribute_399(user, source, value, attribute, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> attribute !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_399(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// attribute O -> attribute !W
	find effectiveJudgement_attribute_399(user, source, value, attribute, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
}
pattern strongConsequenceJudgement_attribute_400(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // attribute W -> attribute R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_attribute_400(user, source, value, attribute, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// attribute !R -> attribute !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_attribute_400(user, source, value, attribute, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> attribute !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_400(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// attribute O -> attribute !W
	find effectiveJudgement_attribute_400(user, source, value, attribute, OperationType::READ, AccessibilityLevel::OBFUSCATE, bound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
}

pattern weakConsequenceJudgement_attribute(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find weakConsequenceJudgement_attribute_weak(user, source, value, attribute, operation, access, bound);
    } or {
    find weakConsequenceJudgement_attribute_19(user, source, value, attribute, operation, access, bound);
    } or {
    find weakConsequenceJudgement_attribute_20(user, source, value, attribute, operation, access, bound);
    } or {
    find weakConsequenceJudgement_attribute_39(user, source, value, attribute, operation, access, bound);
    } or {
    find weakConsequenceJudgement_attribute_40(user, source, value, attribute, operation, access, bound);
    } or {
    find weakConsequenceJudgement_attribute_159(user, source, value, attribute, operation, access, bound);
    } or {
    find weakConsequenceJudgement_attribute_160(user, source, value, attribute, operation, access, bound);
    } or {
    find weakConsequenceJudgement_attribute_179(user, source, value, attribute, operation, access, bound);
    } or {
    find weakConsequenceJudgement_attribute_180(user, source, value, attribute, operation, access, bound);
    } or {
    find weakConsequenceJudgement_attribute_199(user, source, value, attribute, operation, access, bound);
    } or {
    find weakConsequenceJudgement_attribute_200(user, source, value, attribute, operation, access, bound);
    } or {
    find weakConsequenceJudgement_attribute_239(user, source, value, attribute, operation, access, bound);
    } or {
    find weakConsequenceJudgement_attribute_240(user, source, value, attribute, operation, access, bound);
    } or {
    find weakConsequenceJudgement_attribute_259(user, source, value, attribute, operation, access, bound);
    } or {
    find weakConsequenceJudgement_attribute_260(user, source, value, attribute, operation, access, bound);
    } or {
    find weakConsequenceJudgement_attribute_279(user, source, value, attribute, operation, access, bound);
    } or {
    find weakConsequenceJudgement_attribute_280(user, source, value, attribute, operation, access, bound);
    } or {
    find weakConsequenceJudgement_attribute_359(user, source, value, attribute, operation, access, bound);
    } or {
    find weakConsequenceJudgement_attribute_360(user, source, value, attribute, operation, access, bound);
    } or {
    find weakConsequenceJudgement_attribute_379(user, source, value, attribute, operation, access, bound);
    } or {
    find weakConsequenceJudgement_attribute_380(user, source, value, attribute, operation, access, bound);
    } or {
    find weakConsequenceJudgement_attribute_399(user, source, value, attribute, operation, access, bound);
    } or {
    find weakConsequenceJudgement_attribute_400(user, source, value, attribute, operation, access, bound);
}

pattern weakConsequenceJudgement_attribute_weak(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_weak(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
    find attributeAsset(source, value, attribute);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_weak(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}

pattern weakConsequenceJudgement_attribute_19(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_19(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
    find attributeAsset(source, value, attribute);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_19(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_attribute_20(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_20(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
    find attributeAsset(source, value, attribute);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_20(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_attribute_39(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_39(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
    find attributeAsset(source, value, attribute);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_39(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_attribute_40(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_40(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
    find attributeAsset(source, value, attribute);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_40(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_attribute_159(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_159(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
    find attributeAsset(source, value, attribute);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_159(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_attribute_160(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_160(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
    find attributeAsset(source, value, attribute);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_160(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_attribute_179(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_179(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
    find attributeAsset(source, value, attribute);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_179(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_attribute_180(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_180(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
    find attributeAsset(source, value, attribute);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_180(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_attribute_199(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_199(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
    find attributeAsset(source, value, attribute);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_199(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_attribute_200(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_200(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
    find attributeAsset(source, value, attribute);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_200(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_attribute_239(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_239(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
    find attributeAsset(source, value, attribute);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_239(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_attribute_240(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_240(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
    find attributeAsset(source, value, attribute);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_240(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_attribute_259(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_259(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
    find attributeAsset(source, value, attribute);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_259(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_attribute_260(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_260(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
    find attributeAsset(source, value, attribute);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_260(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_attribute_279(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_279(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
    find attributeAsset(source, value, attribute);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_279(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_attribute_280(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_280(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
    find attributeAsset(source, value, attribute);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_280(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_attribute_359(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_359(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
    find attributeAsset(source, value, attribute);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_359(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_attribute_360(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_360(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
    find attributeAsset(source, value, attribute);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_360(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_attribute_379(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_379(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
    find attributeAsset(source, value, attribute);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_379(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_attribute_380(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_380(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
    find attributeAsset(source, value, attribute);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_380(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_attribute_399(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_399(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
    find attributeAsset(source, value, attribute);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_399(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_attribute_400(user: java String, source: EObject, value: java Object, attribute : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_400(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
    find attributeAsset(source, value, attribute);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_400(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find attributeAsset(source, value, attribute);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}

pattern explicitJudgement_reference(user : java String, source : EObject, target : EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType, priority: java Integer)
{
    //  rule hideConsumptionByProtected
    find hideConsumptionByProtectedRuleAsset(source, target, reference);
    find hideConsumptionByProtectedRuleUser(user);
    find readWriteOperation(operation);
    access == AccessibilityLevel::DENY;
    find hideConsumptionByProtectedRuleBoundAndPriority(bound, priority);
} or {
    //  rule default
    find referenceAsset(source, target, reference);
    find allUsers(user);
    find readWriteOperation(operation);
    access == AccessibilityLevel::DENY;
    find lowerUpperBound(bound);
    priority == eval(Constants::DEFAULT_PRIORITY);
}
pattern effectiveJudgement_reference(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find effectiveJudgement_reference_default(user, source, target, reference, operation, access, bound);
} or {
    find effectiveJudgement_reference_weak(user, source, target, reference, operation, access, bound);
} or {
    find effectiveJudgement_reference_19(user, source, target, reference, operation, access, bound);
    } or {
    find effectiveJudgement_reference_20(user, source, target, reference, operation, access, bound);
    } or {
    find effectiveJudgement_reference_39(user, source, target, reference, operation, access, bound);
    } or {
    find effectiveJudgement_reference_40(user, source, target, reference, operation, access, bound);
    } or {
    find effectiveJudgement_reference_159(user, source, target, reference, operation, access, bound);
    } or {
    find effectiveJudgement_reference_160(user, source, target, reference, operation, access, bound);
    } or {
    find effectiveJudgement_reference_179(user, source, target, reference, operation, access, bound);
    } or {
    find effectiveJudgement_reference_180(user, source, target, reference, operation, access, bound);
    } or {
    find effectiveJudgement_reference_199(user, source, target, reference, operation, access, bound);
    } or {
    find effectiveJudgement_reference_200(user, source, target, reference, operation, access, bound);
    } or {
    find effectiveJudgement_reference_239(user, source, target, reference, operation, access, bound);
    } or {
    find effectiveJudgement_reference_240(user, source, target, reference, operation, access, bound);
    } or {
    find effectiveJudgement_reference_259(user, source, target, reference, operation, access, bound);
    } or {
    find effectiveJudgement_reference_260(user, source, target, reference, operation, access, bound);
    } or {
    find effectiveJudgement_reference_279(user, source, target, reference, operation, access, bound);
    } or {
    find effectiveJudgement_reference_280(user, source, target, reference, operation, access, bound);
    } or {
    find effectiveJudgement_reference_359(user, source, target, reference, operation, access, bound);
    } or {
    find effectiveJudgement_reference_360(user, source, target, reference, operation, access, bound);
    } or {
    find effectiveJudgement_reference_379(user, source, target, reference, operation, access, bound);
    } or {
    find effectiveJudgement_reference_380(user, source, target, reference, operation, access, bound);
    } or {
    find effectiveJudgement_reference_399(user, source, target, reference, operation, access, bound);
    } or {
    find effectiveJudgement_reference_400(user, source, target, reference, operation, access, bound);
}
    
pattern effectiveJudgement_reference_default(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
	find judgement_reference_default(user, source, target, reference, operation, access, bound);
	neg find dominationOf_reference_default(user, source, target, reference, operation, access, _dominantBound);
} 	

pattern effectiveJudgement_reference_weak(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
	find judgement_reference_weak(user, source, target, reference, operation, access, bound);
	neg find dominationOf_reference_weak(user, source, target, reference, operation, access, _dominantBound);
} 		

pattern effectiveJudgement_reference_19(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_reference_19(user, source, target, reference, operation, access, bound);
    neg find dominationOf_reference_19(user, source, target, reference, operation, access, _dominantBound);
} 
pattern effectiveJudgement_reference_20(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_reference_20(user, source, target, reference, operation, access, bound);
    neg find dominationOf_reference_20(user, source, target, reference, operation, access, _dominantBound);
} 
pattern effectiveJudgement_reference_39(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_reference_39(user, source, target, reference, operation, access, bound);
    neg find dominationOf_reference_39(user, source, target, reference, operation, access, _dominantBound);
} 
pattern effectiveJudgement_reference_40(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_reference_40(user, source, target, reference, operation, access, bound);
    neg find dominationOf_reference_40(user, source, target, reference, operation, access, _dominantBound);
} 
pattern effectiveJudgement_reference_159(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_reference_159(user, source, target, reference, operation, access, bound);
    neg find dominationOf_reference_159(user, source, target, reference, operation, access, _dominantBound);
} 
pattern effectiveJudgement_reference_160(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_reference_160(user, source, target, reference, operation, access, bound);
    neg find dominationOf_reference_160(user, source, target, reference, operation, access, _dominantBound);
} 
pattern effectiveJudgement_reference_179(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_reference_179(user, source, target, reference, operation, access, bound);
    neg find dominationOf_reference_179(user, source, target, reference, operation, access, _dominantBound);
} 
pattern effectiveJudgement_reference_180(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_reference_180(user, source, target, reference, operation, access, bound);
    neg find dominationOf_reference_180(user, source, target, reference, operation, access, _dominantBound);
} 
pattern effectiveJudgement_reference_199(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_reference_199(user, source, target, reference, operation, access, bound);
    neg find dominationOf_reference_199(user, source, target, reference, operation, access, _dominantBound);
} 
pattern effectiveJudgement_reference_200(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_reference_200(user, source, target, reference, operation, access, bound);
    neg find dominationOf_reference_200(user, source, target, reference, operation, access, _dominantBound);
} 
pattern effectiveJudgement_reference_239(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_reference_239(user, source, target, reference, operation, access, bound);
    neg find dominationOf_reference_239(user, source, target, reference, operation, access, _dominantBound);
} 
pattern effectiveJudgement_reference_240(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_reference_240(user, source, target, reference, operation, access, bound);
    neg find dominationOf_reference_240(user, source, target, reference, operation, access, _dominantBound);
} 
pattern effectiveJudgement_reference_259(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_reference_259(user, source, target, reference, operation, access, bound);
    neg find dominationOf_reference_259(user, source, target, reference, operation, access, _dominantBound);
} 
pattern effectiveJudgement_reference_260(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_reference_260(user, source, target, reference, operation, access, bound);
    neg find dominationOf_reference_260(user, source, target, reference, operation, access, _dominantBound);
} 
pattern effectiveJudgement_reference_279(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_reference_279(user, source, target, reference, operation, access, bound);
    neg find dominationOf_reference_279(user, source, target, reference, operation, access, _dominantBound);
} 
pattern effectiveJudgement_reference_280(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_reference_280(user, source, target, reference, operation, access, bound);
    neg find dominationOf_reference_280(user, source, target, reference, operation, access, _dominantBound);
} 
pattern effectiveJudgement_reference_359(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_reference_359(user, source, target, reference, operation, access, bound);
    neg find dominationOf_reference_359(user, source, target, reference, operation, access, _dominantBound);
} 
pattern effectiveJudgement_reference_360(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_reference_360(user, source, target, reference, operation, access, bound);
    neg find dominationOf_reference_360(user, source, target, reference, operation, access, _dominantBound);
} 
pattern effectiveJudgement_reference_379(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_reference_379(user, source, target, reference, operation, access, bound);
    neg find dominationOf_reference_379(user, source, target, reference, operation, access, _dominantBound);
} 
pattern effectiveJudgement_reference_380(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_reference_380(user, source, target, reference, operation, access, bound);
    neg find dominationOf_reference_380(user, source, target, reference, operation, access, _dominantBound);
} 
pattern effectiveJudgement_reference_399(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_reference_399(user, source, target, reference, operation, access, bound);
    neg find dominationOf_reference_399(user, source, target, reference, operation, access, _dominantBound);
} 
pattern effectiveJudgement_reference_400(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType) 
{
    find judgement_reference_400(user, source, target, reference, operation, access, bound);
} 

pattern dominationOf_reference_default(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
	find dominationBy_reference_weak(user, source, target, reference, operation, dominatedAccess, dominantAccess);
	} or {
    find dominationBy_reference_19(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_20(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_39(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_40(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_159(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_160(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_179(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_180(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_199(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_200(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_239(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_240(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_259(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_260(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_279(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_280(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_359(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_360(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_379(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_380(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_399(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_400(user, source, target, reference, operation, dominatedAccess, dominantAccess);
} 

pattern dominationOf_reference_weak(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_reference_19(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_20(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_39(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_40(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_159(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_160(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_179(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_180(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_199(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_200(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_239(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_240(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_259(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_260(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_279(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_280(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_359(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_360(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_379(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_380(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_399(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_400(user, source, target, reference, operation, dominatedAccess, dominantAccess);
} 

pattern dominationOf_reference_19(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_reference_20(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_39(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_40(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_159(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_160(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_179(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_180(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_199(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_200(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_239(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_240(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_259(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_260(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_279(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_280(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_359(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_360(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_379(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_380(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_399(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_400(user, source, target, reference, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_reference_20(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_reference_39(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_40(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_159(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_160(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_179(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_180(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_199(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_200(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_239(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_240(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_259(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_260(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_279(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_280(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_359(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_360(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_379(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_380(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_399(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_400(user, source, target, reference, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_reference_39(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_reference_40(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_159(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_160(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_179(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_180(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_199(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_200(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_239(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_240(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_259(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_260(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_279(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_280(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_359(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_360(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_379(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_380(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_399(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_400(user, source, target, reference, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_reference_40(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_reference_159(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_160(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_179(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_180(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_199(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_200(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_239(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_240(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_259(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_260(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_279(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_280(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_359(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_360(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_379(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_380(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_399(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_400(user, source, target, reference, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_reference_159(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_reference_160(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_179(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_180(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_199(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_200(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_239(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_240(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_259(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_260(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_279(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_280(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_359(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_360(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_379(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_380(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_399(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_400(user, source, target, reference, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_reference_160(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_reference_179(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_180(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_199(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_200(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_239(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_240(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_259(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_260(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_279(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_280(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_359(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_360(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_379(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_380(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_399(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_400(user, source, target, reference, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_reference_179(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_reference_180(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_199(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_200(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_239(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_240(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_259(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_260(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_279(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_280(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_359(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_360(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_379(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_380(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_399(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_400(user, source, target, reference, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_reference_180(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_reference_199(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_200(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_239(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_240(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_259(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_260(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_279(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_280(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_359(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_360(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_379(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_380(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_399(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_400(user, source, target, reference, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_reference_199(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_reference_200(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_239(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_240(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_259(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_260(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_279(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_280(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_359(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_360(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_379(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_380(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_399(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_400(user, source, target, reference, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_reference_200(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_reference_239(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_240(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_259(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_260(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_279(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_280(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_359(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_360(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_379(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_380(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_399(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_400(user, source, target, reference, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_reference_239(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_reference_240(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_259(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_260(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_279(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_280(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_359(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_360(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_379(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_380(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_399(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_400(user, source, target, reference, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_reference_240(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_reference_259(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_260(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_279(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_280(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_359(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_360(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_379(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_380(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_399(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_400(user, source, target, reference, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_reference_259(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_reference_260(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_279(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_280(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_359(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_360(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_379(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_380(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_399(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_400(user, source, target, reference, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_reference_260(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_reference_279(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_280(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_359(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_360(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_379(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_380(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_399(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_400(user, source, target, reference, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_reference_279(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_reference_280(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_359(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_360(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_379(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_380(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_399(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_400(user, source, target, reference, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_reference_280(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_reference_359(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_360(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_379(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_380(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_399(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_400(user, source, target, reference, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_reference_359(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_reference_360(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_379(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_380(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_399(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_400(user, source, target, reference, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_reference_360(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_reference_379(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_380(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_399(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_400(user, source, target, reference, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_reference_379(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_reference_380(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_399(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_400(user, source, target, reference, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_reference_380(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_reference_399(user, source, target, reference, operation, dominatedAccess, dominantAccess);
    } or {
    find dominationBy_reference_400(user, source, target, reference, operation, dominatedAccess, dominantAccess);
} 
pattern dominationOf_reference_399(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find dominationBy_reference_400(user, source, target, reference, operation, dominatedAccess, dominantAccess);
} 
pattern dominationBy_reference_weak(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_reference_weak(user, source, target, reference, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  

pattern dominationBy_reference_19(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_reference_19(user, source, target, reference, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_reference_20(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_reference_20(user, source, target, reference, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_reference_39(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_reference_39(user, source, target, reference, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_reference_40(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_reference_40(user, source, target, reference, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_reference_159(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_reference_159(user, source, target, reference, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_reference_160(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_reference_160(user, source, target, reference, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_reference_179(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_reference_179(user, source, target, reference, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_reference_180(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_reference_180(user, source, target, reference, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_reference_199(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_reference_199(user, source, target, reference, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_reference_200(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_reference_200(user, source, target, reference, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_reference_239(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_reference_239(user, source, target, reference, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_reference_240(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_reference_240(user, source, target, reference, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_reference_259(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_reference_259(user, source, target, reference, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_reference_260(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_reference_260(user, source, target, reference, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_reference_279(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_reference_279(user, source, target, reference, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_reference_280(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_reference_280(user, source, target, reference, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_reference_359(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_reference_359(user, source, target, reference, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_reference_360(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_reference_360(user, source, target, reference, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_reference_379(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_reference_379(user, source, target, reference, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_reference_380(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_reference_380(user, source, target, reference, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_reference_399(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_reference_399(user, source, target, reference, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  
pattern dominationBy_reference_400(user: java String, source: EObject, target: EObject, reference: java String, operation: OperationType, dominatedAccess: AccessibilityLevel, dominantAccess: AccessibilityLevel) 
{
    find effectiveJudgement_reference_400(user, source, target, reference, operation, dominantAccess, dominantBound);
    find permissionOutOfBound(dominantBound, dominantAccess, dominatedAccess);
}  

pattern judgement_reference_default(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find explicitJudgement_reference(user, source, target, reference, operation, access, bound, eval(Constants::DEFAULT_PRIORITY));
} or {
	find relaxedJudgement_reference_default(user, source, target, reference, operation, access, bound);
}

pattern judgement_reference_weak(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find relaxedJudgement_reference_weak(user, source, target, reference, operation, access, bound);
} or {
	find strongConsequenceJudgement_reference_weak(user, source, target, reference, operation, access, bound);
} or {
    find weakConsequenceJudgement_reference(user, source, target, reference, operation, access, bound);
}

pattern judgement_reference_19(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find explicitJudgement_reference(user, source, target, reference, operation, access, bound, 19);
	} or {
	find relaxedJudgement_reference_19(user, source, target, reference, operation, access, bound);
	} or {
	find strongConsequenceJudgement_reference_19(user, source, target, reference, operation, access, bound);
}

pattern judgement_reference_20(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find explicitJudgement_reference(user, source, target, reference, operation, access, bound, 20);
	} or {
	find relaxedJudgement_reference_20(user, source, target, reference, operation, access, bound);
	} or {
	find strongConsequenceJudgement_reference_20(user, source, target, reference, operation, access, bound);
}

pattern judgement_reference_39(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find explicitJudgement_reference(user, source, target, reference, operation, access, bound, 39);
	} or {
	find relaxedJudgement_reference_39(user, source, target, reference, operation, access, bound);
	} or {
	find strongConsequenceJudgement_reference_39(user, source, target, reference, operation, access, bound);
}

pattern judgement_reference_40(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find explicitJudgement_reference(user, source, target, reference, operation, access, bound, 40);
	} or {
	find relaxedJudgement_reference_40(user, source, target, reference, operation, access, bound);
	} or {
	find strongConsequenceJudgement_reference_40(user, source, target, reference, operation, access, bound);
}

pattern judgement_reference_159(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find explicitJudgement_reference(user, source, target, reference, operation, access, bound, 159);
	} or {
	find relaxedJudgement_reference_159(user, source, target, reference, operation, access, bound);
	} or {
	find strongConsequenceJudgement_reference_159(user, source, target, reference, operation, access, bound);
}

pattern judgement_reference_160(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find explicitJudgement_reference(user, source, target, reference, operation, access, bound, 160);
	} or {
	find relaxedJudgement_reference_160(user, source, target, reference, operation, access, bound);
	} or {
	find strongConsequenceJudgement_reference_160(user, source, target, reference, operation, access, bound);
}

pattern judgement_reference_179(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find explicitJudgement_reference(user, source, target, reference, operation, access, bound, 179);
	} or {
	find relaxedJudgement_reference_179(user, source, target, reference, operation, access, bound);
	} or {
	find strongConsequenceJudgement_reference_179(user, source, target, reference, operation, access, bound);
}

pattern judgement_reference_180(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find explicitJudgement_reference(user, source, target, reference, operation, access, bound, 180);
	} or {
	find relaxedJudgement_reference_180(user, source, target, reference, operation, access, bound);
	} or {
	find strongConsequenceJudgement_reference_180(user, source, target, reference, operation, access, bound);
}

pattern judgement_reference_199(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find explicitJudgement_reference(user, source, target, reference, operation, access, bound, 199);
	} or {
	find relaxedJudgement_reference_199(user, source, target, reference, operation, access, bound);
	} or {
	find strongConsequenceJudgement_reference_199(user, source, target, reference, operation, access, bound);
}

pattern judgement_reference_200(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find explicitJudgement_reference(user, source, target, reference, operation, access, bound, 200);
	} or {
	find relaxedJudgement_reference_200(user, source, target, reference, operation, access, bound);
	} or {
	find strongConsequenceJudgement_reference_200(user, source, target, reference, operation, access, bound);
}

pattern judgement_reference_239(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find explicitJudgement_reference(user, source, target, reference, operation, access, bound, 239);
	} or {
	find relaxedJudgement_reference_239(user, source, target, reference, operation, access, bound);
	} or {
	find strongConsequenceJudgement_reference_239(user, source, target, reference, operation, access, bound);
}

pattern judgement_reference_240(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find explicitJudgement_reference(user, source, target, reference, operation, access, bound, 240);
	} or {
	find relaxedJudgement_reference_240(user, source, target, reference, operation, access, bound);
	} or {
	find strongConsequenceJudgement_reference_240(user, source, target, reference, operation, access, bound);
}

pattern judgement_reference_259(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find explicitJudgement_reference(user, source, target, reference, operation, access, bound, 259);
	} or {
	find relaxedJudgement_reference_259(user, source, target, reference, operation, access, bound);
	} or {
	find strongConsequenceJudgement_reference_259(user, source, target, reference, operation, access, bound);
}

pattern judgement_reference_260(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find explicitJudgement_reference(user, source, target, reference, operation, access, bound, 260);
	} or {
	find relaxedJudgement_reference_260(user, source, target, reference, operation, access, bound);
	} or {
	find strongConsequenceJudgement_reference_260(user, source, target, reference, operation, access, bound);
}

pattern judgement_reference_279(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find explicitJudgement_reference(user, source, target, reference, operation, access, bound, 279);
	} or {
	find relaxedJudgement_reference_279(user, source, target, reference, operation, access, bound);
	} or {
	find strongConsequenceJudgement_reference_279(user, source, target, reference, operation, access, bound);
}

pattern judgement_reference_280(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find explicitJudgement_reference(user, source, target, reference, operation, access, bound, 280);
	} or {
	find relaxedJudgement_reference_280(user, source, target, reference, operation, access, bound);
	} or {
	find strongConsequenceJudgement_reference_280(user, source, target, reference, operation, access, bound);
}

pattern judgement_reference_359(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find explicitJudgement_reference(user, source, target, reference, operation, access, bound, 359);
	} or {
	find relaxedJudgement_reference_359(user, source, target, reference, operation, access, bound);
	} or {
	find strongConsequenceJudgement_reference_359(user, source, target, reference, operation, access, bound);
}

pattern judgement_reference_360(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find explicitJudgement_reference(user, source, target, reference, operation, access, bound, 360);
	} or {
	find relaxedJudgement_reference_360(user, source, target, reference, operation, access, bound);
	} or {
	find strongConsequenceJudgement_reference_360(user, source, target, reference, operation, access, bound);
}

pattern judgement_reference_379(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find explicitJudgement_reference(user, source, target, reference, operation, access, bound, 379);
	} or {
	find relaxedJudgement_reference_379(user, source, target, reference, operation, access, bound);
	} or {
	find strongConsequenceJudgement_reference_379(user, source, target, reference, operation, access, bound);
}

pattern judgement_reference_380(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find explicitJudgement_reference(user, source, target, reference, operation, access, bound, 380);
	} or {
	find relaxedJudgement_reference_380(user, source, target, reference, operation, access, bound);
	} or {
	find strongConsequenceJudgement_reference_380(user, source, target, reference, operation, access, bound);
}

pattern judgement_reference_399(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find explicitJudgement_reference(user, source, target, reference, operation, access, bound, 399);
	} or {
	find relaxedJudgement_reference_399(user, source, target, reference, operation, access, bound);
	} or {
	find strongConsequenceJudgement_reference_399(user, source, target, reference, operation, access, bound);
}

pattern judgement_reference_400(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find explicitJudgement_reference(user, source, target, reference, operation, access, bound, 400);
	} or {
	find strongConsequenceJudgement_reference_400(user, source, target, reference, operation, access, bound);
}


pattern relaxedJudgement_reference_default(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_reference_default(user, source, target, reference, operation, dominatedAccess, bound);
    find dominationOf_reference_default(user, source, target, reference, operation, _dominatedAccess, access);
}

pattern relaxedJudgement_reference_weak(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_reference_weak(user, source, target, reference, operation, dominatedAccess, bound);
    find dominationOf_reference_weak(user, source, target, reference, operation, _dominatedAccess, access);
}

pattern relaxedJudgement_reference_19(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_reference_19(user, source, target, reference, operation, dominatedAccess, bound);
    find dominationOf_reference_19(user, source, target, reference, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_reference_20(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_reference_20(user, source, target, reference, operation, dominatedAccess, bound);
    find dominationOf_reference_20(user, source, target, reference, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_reference_39(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_reference_39(user, source, target, reference, operation, dominatedAccess, bound);
    find dominationOf_reference_39(user, source, target, reference, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_reference_40(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_reference_40(user, source, target, reference, operation, dominatedAccess, bound);
    find dominationOf_reference_40(user, source, target, reference, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_reference_159(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_reference_159(user, source, target, reference, operation, dominatedAccess, bound);
    find dominationOf_reference_159(user, source, target, reference, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_reference_160(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_reference_160(user, source, target, reference, operation, dominatedAccess, bound);
    find dominationOf_reference_160(user, source, target, reference, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_reference_179(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_reference_179(user, source, target, reference, operation, dominatedAccess, bound);
    find dominationOf_reference_179(user, source, target, reference, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_reference_180(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_reference_180(user, source, target, reference, operation, dominatedAccess, bound);
    find dominationOf_reference_180(user, source, target, reference, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_reference_199(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_reference_199(user, source, target, reference, operation, dominatedAccess, bound);
    find dominationOf_reference_199(user, source, target, reference, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_reference_200(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_reference_200(user, source, target, reference, operation, dominatedAccess, bound);
    find dominationOf_reference_200(user, source, target, reference, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_reference_239(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_reference_239(user, source, target, reference, operation, dominatedAccess, bound);
    find dominationOf_reference_239(user, source, target, reference, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_reference_240(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_reference_240(user, source, target, reference, operation, dominatedAccess, bound);
    find dominationOf_reference_240(user, source, target, reference, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_reference_259(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_reference_259(user, source, target, reference, operation, dominatedAccess, bound);
    find dominationOf_reference_259(user, source, target, reference, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_reference_260(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_reference_260(user, source, target, reference, operation, dominatedAccess, bound);
    find dominationOf_reference_260(user, source, target, reference, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_reference_279(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_reference_279(user, source, target, reference, operation, dominatedAccess, bound);
    find dominationOf_reference_279(user, source, target, reference, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_reference_280(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_reference_280(user, source, target, reference, operation, dominatedAccess, bound);
    find dominationOf_reference_280(user, source, target, reference, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_reference_359(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_reference_359(user, source, target, reference, operation, dominatedAccess, bound);
    find dominationOf_reference_359(user, source, target, reference, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_reference_360(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_reference_360(user, source, target, reference, operation, dominatedAccess, bound);
    find dominationOf_reference_360(user, source, target, reference, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_reference_379(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_reference_379(user, source, target, reference, operation, dominatedAccess, bound);
    find dominationOf_reference_379(user, source, target, reference, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_reference_380(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_reference_380(user, source, target, reference, operation, dominatedAccess, bound);
    find dominationOf_reference_380(user, source, target, reference, operation, _dominatedAccess, access);
}
pattern relaxedJudgement_reference_399(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    find judgement_reference_399(user, source, target, reference, operation, dominatedAccess, bound);
    find dominationOf_reference_399(user, source, target, reference, operation, _dominatedAccess, access);
}
pattern strongConsequenceJudgement_reference_weak(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // reference W -> reference R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_reference_weak(user, source, target, reference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// object R -> containment reference R
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_weak(user, target, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object W -> containment reference W
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_weak(user, target, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// reference !R -> reference !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_reference_weak(user, source, target, reference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> incoming reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_weak(user, target, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> outgoing reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_weak(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}

pattern strongConsequenceJudgement_reference_19(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // reference W -> reference R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_reference_19(user, source, target, reference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// object R -> containment reference R
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_19(user, target, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object W -> containment reference W
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_19(user, target, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// reference !R -> reference !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_reference_19(user, source, target, reference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> incoming reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_19(user, target, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> outgoing reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_19(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern strongConsequenceJudgement_reference_20(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // reference W -> reference R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_reference_20(user, source, target, reference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// object R -> containment reference R
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_20(user, target, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object W -> containment reference W
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_20(user, target, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// reference !R -> reference !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_reference_20(user, source, target, reference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> incoming reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_20(user, target, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> outgoing reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_20(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern strongConsequenceJudgement_reference_39(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // reference W -> reference R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_reference_39(user, source, target, reference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// object R -> containment reference R
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_39(user, target, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object W -> containment reference W
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_39(user, target, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// reference !R -> reference !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_reference_39(user, source, target, reference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> incoming reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_39(user, target, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> outgoing reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_39(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern strongConsequenceJudgement_reference_40(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // reference W -> reference R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_reference_40(user, source, target, reference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// object R -> containment reference R
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_40(user, target, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object W -> containment reference W
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_40(user, target, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// reference !R -> reference !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_reference_40(user, source, target, reference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> incoming reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_40(user, target, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> outgoing reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_40(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern strongConsequenceJudgement_reference_159(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // reference W -> reference R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_reference_159(user, source, target, reference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// object R -> containment reference R
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_159(user, target, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object W -> containment reference W
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_159(user, target, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// reference !R -> reference !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_reference_159(user, source, target, reference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> incoming reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_159(user, target, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> outgoing reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_159(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern strongConsequenceJudgement_reference_160(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // reference W -> reference R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_reference_160(user, source, target, reference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// object R -> containment reference R
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_160(user, target, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object W -> containment reference W
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_160(user, target, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// reference !R -> reference !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_reference_160(user, source, target, reference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> incoming reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_160(user, target, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> outgoing reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_160(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern strongConsequenceJudgement_reference_179(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // reference W -> reference R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_reference_179(user, source, target, reference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// object R -> containment reference R
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_179(user, target, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object W -> containment reference W
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_179(user, target, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// reference !R -> reference !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_reference_179(user, source, target, reference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> incoming reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_179(user, target, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> outgoing reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_179(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern strongConsequenceJudgement_reference_180(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // reference W -> reference R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_reference_180(user, source, target, reference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// object R -> containment reference R
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_180(user, target, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object W -> containment reference W
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_180(user, target, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// reference !R -> reference !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_reference_180(user, source, target, reference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> incoming reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_180(user, target, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> outgoing reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_180(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern strongConsequenceJudgement_reference_199(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // reference W -> reference R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_reference_199(user, source, target, reference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// object R -> containment reference R
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_199(user, target, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object W -> containment reference W
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_199(user, target, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// reference !R -> reference !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_reference_199(user, source, target, reference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> incoming reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_199(user, target, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> outgoing reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_199(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern strongConsequenceJudgement_reference_200(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // reference W -> reference R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_reference_200(user, source, target, reference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// object R -> containment reference R
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_200(user, target, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object W -> containment reference W
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_200(user, target, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// reference !R -> reference !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_reference_200(user, source, target, reference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> incoming reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_200(user, target, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> outgoing reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_200(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern strongConsequenceJudgement_reference_239(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // reference W -> reference R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_reference_239(user, source, target, reference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// object R -> containment reference R
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_239(user, target, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object W -> containment reference W
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_239(user, target, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// reference !R -> reference !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_reference_239(user, source, target, reference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> incoming reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_239(user, target, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> outgoing reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_239(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern strongConsequenceJudgement_reference_240(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // reference W -> reference R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_reference_240(user, source, target, reference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// object R -> containment reference R
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_240(user, target, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object W -> containment reference W
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_240(user, target, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// reference !R -> reference !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_reference_240(user, source, target, reference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> incoming reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_240(user, target, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> outgoing reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_240(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern strongConsequenceJudgement_reference_259(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // reference W -> reference R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_reference_259(user, source, target, reference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// object R -> containment reference R
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_259(user, target, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object W -> containment reference W
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_259(user, target, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// reference !R -> reference !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_reference_259(user, source, target, reference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> incoming reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_259(user, target, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> outgoing reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_259(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern strongConsequenceJudgement_reference_260(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // reference W -> reference R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_reference_260(user, source, target, reference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// object R -> containment reference R
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_260(user, target, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object W -> containment reference W
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_260(user, target, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// reference !R -> reference !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_reference_260(user, source, target, reference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> incoming reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_260(user, target, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> outgoing reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_260(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern strongConsequenceJudgement_reference_279(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // reference W -> reference R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_reference_279(user, source, target, reference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// object R -> containment reference R
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_279(user, target, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object W -> containment reference W
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_279(user, target, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// reference !R -> reference !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_reference_279(user, source, target, reference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> incoming reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_279(user, target, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> outgoing reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_279(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern strongConsequenceJudgement_reference_280(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // reference W -> reference R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_reference_280(user, source, target, reference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// object R -> containment reference R
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_280(user, target, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object W -> containment reference W
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_280(user, target, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// reference !R -> reference !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_reference_280(user, source, target, reference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> incoming reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_280(user, target, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> outgoing reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_280(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern strongConsequenceJudgement_reference_359(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // reference W -> reference R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_reference_359(user, source, target, reference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// object R -> containment reference R
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_359(user, target, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object W -> containment reference W
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_359(user, target, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// reference !R -> reference !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_reference_359(user, source, target, reference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> incoming reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_359(user, target, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> outgoing reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_359(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern strongConsequenceJudgement_reference_360(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // reference W -> reference R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_reference_360(user, source, target, reference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// object R -> containment reference R
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_360(user, target, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object W -> containment reference W
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_360(user, target, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// reference !R -> reference !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_reference_360(user, source, target, reference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> incoming reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_360(user, target, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> outgoing reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_360(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern strongConsequenceJudgement_reference_379(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // reference W -> reference R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_reference_379(user, source, target, reference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// object R -> containment reference R
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_379(user, target, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object W -> containment reference W
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_379(user, target, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// reference !R -> reference !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_reference_379(user, source, target, reference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> incoming reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_379(user, target, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> outgoing reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_379(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern strongConsequenceJudgement_reference_380(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // reference W -> reference R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_reference_380(user, source, target, reference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// object R -> containment reference R
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_380(user, target, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object W -> containment reference W
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_380(user, target, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// reference !R -> reference !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_reference_380(user, source, target, reference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> incoming reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_380(user, target, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> outgoing reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_380(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern strongConsequenceJudgement_reference_399(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // reference W -> reference R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_reference_399(user, source, target, reference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// object R -> containment reference R
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_399(user, target, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object W -> containment reference W
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_399(user, target, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// reference !R -> reference !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_reference_399(user, source, target, reference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> incoming reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_399(user, target, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> outgoing reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_399(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern strongConsequenceJudgement_reference_400(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
    // reference W -> reference R
    dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_reference_400(user, source, target, reference, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
    operation == OperationType::READ;
    access == AccessibilityLevel::ALLOW;
    bound == eval(BoundType::LOWER);
} or {
	// object R -> containment reference R
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_400(user, target, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// object W -> containment reference W
	dominantBound == eval(BoundType::LOWER);
    find effectiveJudgement_object_400(user, target, OperationType::WRITE, AccessibilityLevel::ALLOW, dominantBound);
	find containmentReference(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	// reference !R -> reference !W
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_reference_400(user, source, target, reference, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> incoming reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_400(user, target, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
} or {
	// object !R -> outgoing reference !R
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_400(user, source, OperationType::READ, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}

pattern weakConsequenceJudgement_reference(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	find weakConsequenceJudgement_reference_weak(user, source, target, reference, operation, access, bound);
    } or {
    find weakConsequenceJudgement_reference_19(user, source, target, reference, operation, access, bound);
    } or {
    find weakConsequenceJudgement_reference_20(user, source, target, reference, operation, access, bound);
    } or {
    find weakConsequenceJudgement_reference_39(user, source, target, reference, operation, access, bound);
    } or {
    find weakConsequenceJudgement_reference_40(user, source, target, reference, operation, access, bound);
    } or {
    find weakConsequenceJudgement_reference_159(user, source, target, reference, operation, access, bound);
    } or {
    find weakConsequenceJudgement_reference_160(user, source, target, reference, operation, access, bound);
    } or {
    find weakConsequenceJudgement_reference_179(user, source, target, reference, operation, access, bound);
    } or {
    find weakConsequenceJudgement_reference_180(user, source, target, reference, operation, access, bound);
    } or {
    find weakConsequenceJudgement_reference_199(user, source, target, reference, operation, access, bound);
    } or {
    find weakConsequenceJudgement_reference_200(user, source, target, reference, operation, access, bound);
    } or {
    find weakConsequenceJudgement_reference_239(user, source, target, reference, operation, access, bound);
    } or {
    find weakConsequenceJudgement_reference_240(user, source, target, reference, operation, access, bound);
    } or {
    find weakConsequenceJudgement_reference_259(user, source, target, reference, operation, access, bound);
    } or {
    find weakConsequenceJudgement_reference_260(user, source, target, reference, operation, access, bound);
    } or {
    find weakConsequenceJudgement_reference_279(user, source, target, reference, operation, access, bound);
    } or {
    find weakConsequenceJudgement_reference_280(user, source, target, reference, operation, access, bound);
    } or {
    find weakConsequenceJudgement_reference_359(user, source, target, reference, operation, access, bound);
    } or {
    find weakConsequenceJudgement_reference_360(user, source, target, reference, operation, access, bound);
    } or {
    find weakConsequenceJudgement_reference_379(user, source, target, reference, operation, access, bound);
    } or {
    find weakConsequenceJudgement_reference_380(user, source, target, reference, operation, access, bound);
    } or {
    find weakConsequenceJudgement_reference_399(user, source, target, reference, operation, access, bound);
    } or {
    find weakConsequenceJudgement_reference_400(user, source, target, reference, operation, access, bound);
}

pattern weakConsequenceJudgement_reference_weak(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_weak(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_weak(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}

pattern weakConsequenceJudgement_reference_19(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_19(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_19(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_reference_20(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_20(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_20(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_reference_39(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_39(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_39(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_reference_40(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_40(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_40(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_reference_159(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_159(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_159(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_reference_160(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_160(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_160(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_reference_179(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_179(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_179(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_reference_180(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_180(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_180(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_reference_199(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_199(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_199(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_reference_200(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_200(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_200(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_reference_239(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_239(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_239(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_reference_240(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_240(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_240(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_reference_259(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_259(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_259(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_reference_260(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_260(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_260(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_reference_279(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_279(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_279(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_reference_280(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_280(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_280(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_reference_359(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_359(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_359(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_reference_360(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_360(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_360(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_reference_379(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_379(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_379(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_reference_380(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_380(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_380(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_reference_399(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_399(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_399(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}
pattern weakConsequenceJudgement_reference_400(user: java String, source: EObject, target: EObject, reference : java String, operation: OperationType, access: AccessibilityLevel, bound: java BoundType)
{
	dominantBound == eval(BoundType::LOWER);
	find effectiveJudgement_object_400(user, source, OperationType::READ, AccessibilityLevel::ALLOW, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::READ;
	access == AccessibilityLevel::ALLOW;
	bound == eval(BoundType::LOWER);
} or {
	dominantBound == eval(BoundType::UPPER);
	find effectiveJudgement_object_400(user, source, OperationType::WRITE, AccessibilityLevel::DENY, dominantBound);
	find referenceAsset(source, target, reference);
	operation == OperationType::WRITE;
	access == AccessibilityLevel::DENY;
	bound == eval(BoundType::UPPER);
}

pattern attributeAsset(source : EObject, value: java Object, attribute : java String) {
	find attributeAssetWTmodel(source, value, attribute);
	} or {
	find attributeAssetSystemFaultvalue(source, value, attribute);
	} or {
	find attributeAssetSystemParamvalue(source, value, attribute);
	} or {
	find attributeAssetSystemParamsettable(source, value, attribute);
	} or {
	find attributeAssetSystemTimervalue(source, value, attribute);
	} or {
	find attributeAssetSubsystemdescription(source, value, attribute);
	} or {
	find attributeAssetControlUnitcycle(source, value, attribute);
	} or {
	find attributeAssetControlUnitpriority(source, value, attribute);
	} or {
	find attributeAssetControlUnitenabled(source, value, attribute);
	} or {
	find attributeAssetControlUnitBhvParams(source, value, attribute);
	} or {
	find attributeAssetOwnershipowner(source, value, attribute);
}

pattern attributeAssetWTmodel(source : EObject, value: java Object, attribute : java String)
{
    WT.model(source, value);
    attribute == "model";
}

pattern attributeAssetSystemFaultvalue(source : EObject, value: java Object, attribute : java String)
{
    SystemFault.value(source, value);
    attribute == "value";
}

pattern attributeAssetSystemParamvalue(source : EObject, value: java Object, attribute : java String)
{
    SystemParam.value(source, value);
    attribute == "value";
}

pattern attributeAssetSystemParamsettable(source : EObject, value: java Object, attribute : java String)
{
    SystemParam.settable(source, value);
    attribute == "settable";
}

pattern attributeAssetSystemTimervalue(source : EObject, value: java Object, attribute : java String)
{
    SystemTimer.value(source, value);
    attribute == "value";
}

pattern attributeAssetSubsystemdescription(source : EObject, value: java Object, attribute : java String)
{
    Subsystem.description(source, value);
    attribute == "description";
}

pattern attributeAssetControlUnitcycle(source : EObject, value: java Object, attribute : java String)
{
    ControlUnit.cycle(source, value);
    attribute == "cycle";
}

pattern attributeAssetControlUnitpriority(source : EObject, value: java Object, attribute : java String)
{
    ControlUnit.priority(source, value);
    attribute == "priority";
}

pattern attributeAssetControlUnitenabled(source : EObject, value: java Object, attribute : java String)
{
    ControlUnit.enabled(source, value);
    attribute == "enabled";
}

pattern attributeAssetControlUnitBhvParams(source : EObject, value: java Object, attribute : java String)
{
    ControlUnit.BhvParams(source, value);
    attribute == "BhvParams";
}

pattern attributeAssetOwnershipowner(source : EObject, value: java Object, attribute : java String)
{
    Ownership.owner(source, value);
    attribute == "owner";
}

pattern referenceAsset(source : EObject, target : EObject, reference : java String) {
	find referenceAssetWTsubsystems(source, target, reference);     
	} or {
	find referenceAssetWTinputs(source, target, reference);     
	} or {
	find referenceAssetWToutputs(source, target, reference);     
	} or {
	find referenceAssetWTparams(source, target, reference);     
	} or {
	find referenceAssetWTtimers(source, target, reference);     
	} or {
	find referenceAssetWTfaults(source, target, reference);     
	} or {
	find referenceAssetWTvariables(source, target, reference);     
	} or {
	find referenceAssetSubsystemcontrolUnits(source, target, reference);     
	} or {
	find referenceAssetSubsystemsubsystems(source, target, reference);     
	} or {
	find referenceAssetControlUnitinputs(source, target, reference);     
	} or {
	find referenceAssetControlUnitoutputs(source, target, reference);     
	} or {
	find referenceAssetControlUnitparams(source, target, reference);     
	} or {
	find referenceAssetControlUnitfault(source, target, reference);     
	} or {
	find referenceAssetControlUnittimers(source, target, reference);     
	} or {
	find referenceAssetAuthorizedSystemwtSystem(source, target, reference);     
	} or {
	find referenceAssetAuthorizedSystemprotected(source, target, reference);     
	} or {
	find referenceAssetAuthorizedSystemownerships(source, target, reference);     
	} or {
	find referenceAssetOwnershipowned(source, target, reference);     
}

pattern referenceAssetWTsubsystems(source : EObject, target : EObject, reference : java String)
{
	WT.subsystems(source, target);
	reference == "subsystems";
}

pattern referenceAssetWTinputs(source : EObject, target : EObject, reference : java String)
{
	WT.inputs(source, target);
	reference == "inputs";
}

pattern referenceAssetWToutputs(source : EObject, target : EObject, reference : java String)
{
	WT.outputs(source, target);
	reference == "outputs";
}

pattern referenceAssetWTparams(source : EObject, target : EObject, reference : java String)
{
	WT.params(source, target);
	reference == "params";
}

pattern referenceAssetWTtimers(source : EObject, target : EObject, reference : java String)
{
	WT.timers(source, target);
	reference == "timers";
}

pattern referenceAssetWTfaults(source : EObject, target : EObject, reference : java String)
{
	WT.faults(source, target);
	reference == "faults";
}

pattern referenceAssetWTvariables(source : EObject, target : EObject, reference : java String)
{
	WT.variables(source, target);
	reference == "variables";
}

pattern referenceAssetSubsystemcontrolUnits(source : EObject, target : EObject, reference : java String)
{
	Subsystem.controlUnits(source, target);
	reference == "controlUnits";
}

pattern referenceAssetSubsystemsubsystems(source : EObject, target : EObject, reference : java String)
{
	Subsystem.subsystems(source, target);
	reference == "subsystems";
}

pattern referenceAssetControlUnitinputs(source : EObject, target : EObject, reference : java String)
{
	ControlUnit.inputs(source, target);
	reference == "inputs";
}

pattern referenceAssetControlUnitoutputs(source : EObject, target : EObject, reference : java String)
{
	ControlUnit.outputs(source, target);
	reference == "outputs";
}

pattern referenceAssetControlUnitparams(source : EObject, target : EObject, reference : java String)
{
	ControlUnit.params(source, target);
	reference == "params";
}

pattern referenceAssetControlUnitfault(source : EObject, target : EObject, reference : java String)
{
	ControlUnit.fault(source, target);
	reference == "fault";
}

pattern referenceAssetControlUnittimers(source : EObject, target : EObject, reference : java String)
{
	ControlUnit.timers(source, target);
	reference == "timers";
}

pattern referenceAssetAuthorizedSystemwtSystem(source : EObject, target : EObject, reference : java String)
{
	AuthorizedSystem.wtSystem(source, target);
	reference == "wtSystem";
}

pattern referenceAssetAuthorizedSystemprotected(source : EObject, target : EObject, reference : java String)
{
	AuthorizedSystem.protected(source, target);
	reference == "protected";
}

pattern referenceAssetAuthorizedSystemownerships(source : EObject, target : EObject, reference : java String)
{
	AuthorizedSystem.ownerships(source, target);
	reference == "ownerships";
}

pattern referenceAssetOwnershipowned(source : EObject, target : EObject, reference : java String)
{
	Ownership.owned(source, target);
	reference == "owned";
}

pattern containmentReference(source : EObject, target : EObject, reference : java String) {
	find referenceAsset(source, target, reference);
	WT(source);
	reference == "subsystems";
	} or {
	find referenceAsset(source, target, reference);
	WT(source);
	reference == "inputs";
	} or {
	find referenceAsset(source, target, reference);
	WT(source);
	reference == "outputs";
	} or {
	find referenceAsset(source, target, reference);
	WT(source);
	reference == "params";
	} or {
	find referenceAsset(source, target, reference);
	WT(source);
	reference == "timers";
	} or {
	find referenceAsset(source, target, reference);
	WT(source);
	reference == "faults";
	} or {
	find referenceAsset(source, target, reference);
	WT(source);
	reference == "variables";
	} or {
	find referenceAsset(source, target, reference);
	Subsystem(source);
	reference == "controlUnits";
	} or {
	find referenceAsset(source, target, reference);
	Subsystem(source);
	reference == "subsystems";
	} or {
	find referenceAsset(source, target, reference);
	AuthorizedSystem(source);
	reference == "wtSystem";
	} or {
	find referenceAsset(source, target, reference);
	AuthorizedSystem(source);
	reference == "ownerships";
}

pattern contains(container: EObject, contained: EObject) {
	WT.subsystems(container, contained);
	} or {
	WT.inputs(container, contained);
	} or {
	WT.outputs(container, contained);
	} or {
	WT.params(container, contained);
	} or {
	WT.timers(container, contained);
	} or {
	WT.faults(container, contained);
	} or {
	WT.variables(container, contained);
	} or {
	Subsystem.controlUnits(container, contained);
	} or {
	Subsystem.subsystems(container, contained);
	} or {
	AuthorizedSystem.wtSystem(container, contained);
	} or {
	AuthorizedSystem.ownerships(container, contained);
}

pattern objectAsset(object: EObject) {
	WT(object);
	} or {
	SystemInput(object);
	} or {
	SystemOutput(object);
	} or {
	SystemFault(object);
	} or {
	SystemParam(object);
	} or {
	SystemTimer(object);
	} or {
	SystemVariable(object);
	} or {
	WTCInput(object);
	} or {
	WTCOutput(object);
	} or {
	WTCParam(object);
	} or {
	WTCTimer(object);
	} or {
	WTCFault(object);
	} or {
	Subsystem(object);
	} or {
	ControlUnit(object);
	} or {
	MainSubsystem(object);
	} or {
	AuthorizedSystem(object);
	} or {
	Ownership(object);
}

pattern root(object: EObject) {
    WT(object);
}

pattern objectAssetWithoutRoot(object: EObject) {
	find objectAsset(object);
	neg find root(object);
}
